# Game Wire 記事カテゴリ仕様書

最終更新: 2026-08-07
対象: 週刊ゲーム情報マガジン「Game Wire」の 1 号に載る記事の**選び方**と**書き方**

---

## 0. このドキュメントについて

### 何が書かれているか

1 号（毎週 1 回発行）に載せる 6 本の記事について、

- どのカテゴリを何本載せるか
- 各カテゴリでどんなゲームを選ぶか（＝**編集意図**）
- その意図をどんなデータ・数値で実現するか（＝**選定ロジック**）
- 記事の中に何を書き、何を書かないか

を定めたものです。コードは載せていません。各セクションの後半にある「▼ ロジック」が、実際に使うデータ項目とパラメータです。

### 検討資料との関係

このドキュメントは **決定した結果だけ**を書いています。「なぜそう決めたか」「他の案をなぜ捨てたか」「どんな実測をしたか」は、検討資料 `docs/article-category-spec-review.md`（3,000 行超）に残してあります。判断を蒸し返す必要が出たときはそちらを参照してください。

### 前提となる読者像

**ゲームクリエイターを目指す学生。** 幅広いジャンル・種類のゲームの情報を、短時間で濃く得たい。

この読者像から、4 つの枠は「面白いゲームの寄せ集め」ではなく **ゲームを見る 4 つの異なる視点**を提供するものとして設計されています。

### 現在のステータス

意思決定はすべて完了しています（§9.1 の保留 2 件を除く）。**実装は未着手**で、これから PR に分割して進めます。→ §8

**2026-08-07 の更新（実装計画をコードと突き合わせて補正）**: §8 の PR 分割をコード実読で検証し、決定済みなのに実装 PR が無かった 3 件を追加しました。

- **PR-B2**（新規）— 国内販売軸のデータ取得。§2.3 が 4 軸の 1 つに据えているのに取得実装が存在しませんでした
- **PR-I**（新規）— インディー枠の規模判定。§3.4 が枠の中核条件に決めているのに PR が割り当てられていませんでした
- **PR-F0**（新規）— 発行日の JST 化。§9.3-2 が「PR-F の前に対応する」としているのに順序表に現れていませんでした

あわせて、§7.1 の指標のうち**実際に取得済みなのは 4 つだけ**であることを明記し（残りはフィールド追加・新規エンドポイント・新規スクレイパが必要）、Amazon 順位の非永続化が中間データファイルにも及ぶことを §2.3 に追記しました。

**2026-08-07 の第 2 次更新（ライブ API による未検証依存の一括実測）**: 仕様が依存するデータ源を、推論ではなく実測で検証しました。**仕様の判断はおおむね妥当でしたが、実装上の欠陥が 2 件見つかりました。**

| 検証対象 | 結果 |
|---|---|
| §7.1 の未取得 10 指標 | ✅ すべて取得可能（`date_format` の経路のみ修正） |
| §5.4 名作枠の母集団 | ✅ 257 件（記録は 254 件。誤差の範囲） |
| §3.4 開発本数 | ✅ 引ける。しかも `games` クエリへの **1 フィールド追加**で足りる（別エンドポイント不要） |
| §2.3 ファミ通ランキング | ✅ 今も取得可能。**ノイズを定量化**（1 位が非ゲーム商品） |
| §3.5 Steam おすすめ数 | ✅ 値は取れる（886,088 等）。⚠️ ただし**母集団由来の候補では `steamAppId` が埋まらず実際には取れない**（§3.6。`steamUrl` の抽出漏れ） |
| §2.2 新作枠の供給 | ✅ 発売済み 16 件 + 未発売 29 件（確定日のみ）= **45 件で足りている**。⚠️ ただし **vol.17 の 0 本の原因は特定できていません**（当初「取得段階の混在」と結論したがレビュー指摘で誤りと判明。§2.2 / §9.3-9） |
| **§3.5 YouTube 上位 30%** | ❌ **実際には上位 80% を通すバグ**。かつ候補が 14 件中 1 件しかマッチしない → 経路を廃止（Issue #217 で別途検証） |

⚠️ **不備が見つかったのは「IGDB 以外のデータ源」でした。** 検討資料 3,000 行超の実測は主に IGDB クエリに対して行われており、YouTube・Steam の可用性検証が手薄でした。同種の見落としを避けるため、実装前に依存を実測する手順を §8 の検証方法に追加しています。

**2026-08-07 の第 3 次更新（コードレビュー指摘の反映）**: 上記の実測結果に対するレビューで、**私の測定と結論に 3 つの誤りが見つかりました**。いずれも実測で確認して訂正しました。

| 誤り | 訂正内容 |
|---|---|
| **vol.17 の原因を「取得段階の混在」と結論した** | **誤り**。3 つある母集団クエリのうち 1 本（`fetchRecentPopularGames`）だけを測っていた。マージ後は 60 日窓の発売済みが **19 件**入っており取得段階にボトルネックはない。真の原因は下流にあり**未特定** → §9.3-9 として切り出した |
| **PR-I に `companies` エンドポイントへの新規リクエスト経路を規定した** | **不要だった**。`games` クエリの `involved_companies.company.developed` で取れる（1 フィールド追加）。レート制限の配慮も候補を絞る制約も不要 |
| **インディー枠の並び順キーが大半で欠落する** | レビュー指摘どおり現状は欠落する。ただし原因は「コンソール専売だから」ではなく **`steamUrl` の抽出漏れ**で、候補の **91% は Steam ストア URL を持っている**。抽出を直す修正を PR-I に追加し、タイブレークも規定した → §3.6 |

このほか、未発売枠の件数（33 → 確定日のみ 29）、§7.1 の取得済み指標の数え間違いと hypes の扱い、percentile 修正式の off-by-one を訂正しました。

---

## 1. 1 号の構成

| カテゴリ | 本数 | 一言でいうと | 読者への約束 |
|---|---|---|---|
| **新作紹介** | 2 本 | これから／今 | 「今週チェックすべき新しいゲームはこれ」— 市場の最前線を知る |
| **インディーゲーム** | 2 本 | 作り手の視点 | 「小さなチームがどう作ったか」— 自分が作る側に立ったときの参考 |
| **特集** | 1 本 | 横のつながり | 「今この時期に意味を持つ切り口」— 単体では見えない文脈 |
| **名作深掘り** | 1 本 | 時間に耐えたもの | 「なぜ何年経っても評価されるのか」— 良いゲームの普遍的条件 |

計 **6 本**。

### 1.1 4 つの枠を分ける「軸」の考え方

改修前は、新作紹介とインディーが **企業規模**（大手 / 非大手）で排他になっていました。この構造には欠陥がありました。大手のタイトルが枯れた週は新作紹介が消え、その分がインディー枠に流れ込む——つまり枠が互いの受け皿になっていて、独立していなかったのです（実際に vol.17 で新作紹介が 0 本になりました）。

本仕様では **軸を「ゲームの時間軸」に揃え、企業規模を軸から降格させます**。

- 新作紹介 = **新しいもの**（発売直後・発売直前）
- 名作深掘り = **古くても評価され続けているもの**
- 特集 = **暦の上で今この時期に意味を持つもの**
- インディー = ここだけは**規模が本質的な軸**（「小規模開発の話を聞く枠」だから）

結果として、**企業規模の判定はインディー枠の除外条件としてのみ使います**。新作紹介から「大手であること」という条件は撤廃されます。

> ✅ **実装しました**（2026-08-09。PR #237）。`select-newreleases-with-fallback.ts` の `isLargeStudio` ゲートを削除。
>
> ⚠️ **この決定（論点A = A-1、2026-07-29 決着）は 2026-08-09 まで実装されていませんでした。** §8 の PR 一覧のどこにも割り当てられていなかったためです。その間、実データで **Steam Top Sellers 1 位の『ほの暮しの庭』（Nippon Ichi Software / NIS America）が新作枠にもインディー枠にも載らない**状態が発生していました。Issue #231 はこの現象を「静的リストの不備」と診断し、**決着で棄却された (A-3)「ゲートを維持して判定精度を上げる」**を提案していました。**決着済みの決定が実装されているかは、Issue の記述とは独立に確認する必要があります。**

---

## 2. 新作紹介（2 本）

### 2.1 読者への約束

「今週、ゲーム業界で何が新しく出たか／出るか」。市場の最前線の定点観測。

### 2.2 何を載せるか

- **発売されたばかりのタイトル**を優先する（過去 60 日以内）
- 発売済みで 2 本埋まらなかったときだけ、**もうすぐ発売のタイトル**で埋める（今後 90 日以内）
- **企業規模は問わない**。大手でもインディーでも「注目されているなら載せる」
- 「発売済み」「未発売」の境界は **日本時間の当日 0 時**。当日発売のタイトルは未発売として扱う（当日はまだユーザーの評価も声も存在しないため）。ただし**見出しだけは発売前提にします** → §2.8
- **リメイク・リマスターも載せる**。ただし記事にその旨を明記させる（→ §6.2）

配分は固定ではなく可変です。発売済みだけで 2 本埋まる週がほとんどで（実測では発売済み候補が 11〜16 件あった）、未発売枠が使われるのは発売済みが枯れた週だけになります。

> 📊 **2026-08-07 ライブ API 実測: 供給は足りています。そして vol.17 の新作 0 本の原因が判明しました。**
>
> **供給量（仕様どおりのクエリで測定）**
>
> | 枠 | 実測 |
> |---|---|
> | 発売済み（60 日窓・品質条件 = 批評媒体数 2 以上 OR 票数 15 以上） | **16 件** |
> | 未発売（90 日窓・hypes 20 超・カバー画像あり） | 33 件。**うち §2.4 の「確定日のみ」を満たすのは 29 件**（開発元は 33 件すべて判明） |
>
> **採用可能な合計は 16 + 29 = 45 件**（§2.4 が確定日のみを採るため、未発売側は 29 件で数えます）。§2.2 のフォールバック（発売済みで埋まらない分を未発売で埋める）が効くため、**両方が同時に枯れることは考えにくい**です。未発売側の先頭は `Duskfade`（8/13）・`The Sinking City 2`（8/18）・`Mortal Shell II`（8/20）で、「もうすぐ発売」として妥当な内容でした。
>
> **現在の実装の問題（ただし当初の分析は誤りでした。2026-08-07 のレビュー指摘を受けて訂正）**
>
> 母集団クエリ `fetchRecentPopularGames` は `first_release_date > 90日前` と書かれており**発売日の上限がありません**。過去 90 日と未来の全タイトルが同じ袋に入り、`sort hypes desc` + `limit 20` で切られます。hypes は定義上「発売前フォロー数」なので未発売作が構造的に上位を占めます（実測: 20 件中 発売済み 4 件 / 未発売 16 件。2028 年発売の `Tomb Raider: Catalyst` まで枠を占領）。
>
> **⚠️ しかし「だから取得段階で 4 件しか入らない」という当初の結論は誤りでした。** `fetchIGDBData` は **3 つのクエリを並列実行して id で重複除去マージ**します（`fetchRecentPopularGames` limit 20 / `fetchClassicGames` limit 30 / `fetchIndieGames` limit 50）。私が測ったのは 1 本目だけでした。マージ後の実測:
>
> ```
> マージ後の総数 75 件（発売済み 55 / 未発売 17）
> うち 60 日窓の発売済み: 19 件
>   Splatoon Raiders / Palworld / Fields of Mistria / Rhythm Heaven Groove …
> ```
>
> **取得段階にボトルネックはありません。** したがって「§2.3 / §2.4 の分離で構造的に解消される」という結論は**裏付けがありません**。分離自体は仕様として妥当（枠ごとに条件と並び順を分けるため）ですが、それが vol.17 の再発防止になるとは言えません。
>
> **vol.17 の真の原因は下流にあると考えられます**（未確定）。候補が母集団に入った後、`isQualifiedGame` / `hasExistenceEvidence` / 大手判定ゲート / クールダウン 17 週 / 完全性ゲート（カバー画像・開発元・ソース URL 必須）を通過する必要があります。**どの段階で何件落ちるかは未測定です。** PR-B / PR-G の着手前に、この通過率をライブで測って特定する必要があります（→ §9.3-9）。

### 2.3 ▼ ロジック: 発売済み側

**母集団の条件**（IGDB から取得）

| 項目 | 値 |
|---|---|
| 発売日 | 日本時間当日 0 時の 60 日前 〜 当日 0 時（未満） |
| ゲーム種別 | Main Game・リメイク・リマスター（DLC・拡張・バンドル・移植を除外） |
| テーマ除外 | 成人向け（Erotic）を除外 |

**品質条件**（次のいずれか 1 つを満たせば通過）

| 条件 | 意味 |
|---|---|
| 批評媒体数が 2 以上 | 2 社以上のレビューがある（✅ 実装済み: 2026-08-09、PR #249。`isQualifiedGame` に `aggregated_rating_count >= QUALITY_CRITIC_COUNT_MIN` 経路を追加） |
| IGDB ユーザー投票数が 15 以上 | ユーザー評価が一定数ある（実装済み） |
| Steam Top Sellers に載っている | 売れている（実装済み） |
| Amazon 国内ランキング（ゲームソフト）に載っている | 国内で売れている（✅ 実装済み: 2026-08-09、PR #249。新作枠にのみ有効化。§9.2-4 の照合方式で `AmazonRankIndex` を経路として接続） |
| IGDB ユーザー投票数が 8 以上、かつ IGDB ユーザー評価が 85 以上 | 条件2（投票数 15 以上）の**閾値緩和版**。投票数がより少なくても、評価が非常に高ければ通す（✅ 実装済み。コードは以前から存在し、PR #258（2026-08-10、マージ `0af2a36`）で仕様外経路を整理した際に、削除せず本条件として明文化する方針を確定した。実データで高評価・低投票数タイトル（例: Splatoon Raiders）を正しく救済していることを確認済み） |

**並び順: 4 軸スコアの「重み付き最大値」**

4 つの軸それぞれを 0〜100 点の**絶対尺度**に変換し、**そのゲームが最も高い点を取った軸の点数**をスコアとします（合計ではなく最大値）。データを持たない軸は「棄権」で、0 点扱いにはしません。

| 軸 | 使えるための条件 | 0〜100 への変換 | 100 点の意味 | 初期の重み |
|---|---|---|---|---|
| 批評 | 批評媒体数 2 以上 | 批評平均点 × min(1, 媒体数 ÷ 4) | 4 媒体以上の平均が 100 点 | 1.0 |
| ユーザー票数 | 投票数 15 以上 | min(100, 100 × log10(投票数) ÷ log10(500)) | IGDB で 500 票 | 1.0 |
| Steam | Top Sellers 掲載 | 100 ×(1 −(順位 − 1)÷ 掲載枠数) | Top Sellers 1 位 | 1.0 |
| 国内販売 | Amazon ランキング掲載 | 100 ×(1 −(順位 − 1)÷ 50) | Amazon ゲームソフト 1 位 | 1.0 |

補足:

- **批評軸に「媒体数 ÷ 4」の補正をかける理由**: 批評 1 社だけの 90 点と 10 社平均の 90 点は信頼度が違います。4 媒体で満点になるよう割り引きます。
- **ユーザー票数を対数にする理由**: 500 票と 5,000 票の差は、15 票と 150 票の差ほど重要ではありません。
- **ユーザー票数軸に上限（100 点でのクリップ）をかける理由**: 対数式は 500 票を超えると 100 を超えます（1,000 票で 111、5,000 票で **137**。2026-08-08 訂正。当初「133」と書いていたのは計算違いで、`100 × log10(5000) ÷ log10(500) = 137.05` です）。他の 3 軸は構造的に 100 が上限なので、クリップしないとこの軸だけが 100 超に到達でき、**最大値集約のもとでは票数軸で突き抜けたタイトルが常に勝つ**構造になります。「1 つの軸で突き抜けているタイトルが正しく上に来る」という意図（下記）を満たすには、軸間の物差しが揃っている必要があります。500 票以上を一律 100 点として区別しないことは、上記の「500 票と 5,000 票の差は重要ではない」という方針とも整合します。
- **順位を線形にした理由**: 対数・逆数・平方根も検証しましたが、重みを少し変えると順位が入れ替わる不安定さがありました。線形が最も安定していました。
- **合計ではなく最大値を採る理由**: 合計にすると「どの軸も凡庸だが軸を多く持っているだけのタイトル」が上位に来ます。最大値なら「1 つの軸で突き抜けているタイトル」が正しく上に来ます。
- **重みは環境変数で調整できるようにします。** 初期値はすべて 1.0 で、運用しながら再調整します（Issue #210）。

**国内販売軸の重要な制約**

データ源はファミ通サイト経由の Amazon ゲームソフトランキングです。**順位は選定の内部計算にだけ使い、記事本文や記事データには一切出力しません。** Amazon のライセンス（24 時間を超える保存の禁止）とファミ通の著作権条項（蓄積の禁止）に触れる可能性があるためです。

**この「出力しない」は中間データファイルにも及びます。** `data/aggregated.json` と `data/selected-games.json` は Git の追跡下にあり、前者の `games[]` は `GameData` をそのまま直列化しています（`steamRank` が実際に保存されています）。したがって**順位を `GameData` に持たせると、コミットされた時点で恒久保存になり** 24 時間制限と正面から衝突します。順位は選定処理のスコープ内に閉じ、永続化される構造体（`GameData` / `aggregated.json` / `selected-games.json` / 記事 frontmatter）には載せません。**取得経路は実装済みです**（`scripts/fetch-amazon-ranking.ts`。2026-08-09、PR #249）。実装時のレビューで、フィールド名だけでなく**並び順からも順位が漏れる導出チャネルが 2 つ**見つかりました（`newReleasesReserves` のスコア降順、候補ログのスコア降順）。前者は `selected-games.json` への書き出し対象から `newReleasesReserves` を除外して塞ぎ、後者はログ出力をタイトル昇順に変更しました（選定に使う配列の順序は不変）。詳細は `docs/article-category-prompt.md` の PR-B2「実施結果」節を参照してください。

**この制約の対象は「毎週の実行が自動生成し蓄積していく成果物」です。`docs/` 配下のドキュメントは意図的に対象外とします**（2026-08-09 のユーザー判断）。設計判断の根拠として順位を含む実測値を記録することは認めます。本節の 2026-08-07 / 2026-08-09 の実測ブロックが順位を挙げているのはこの例外に拠るものです。対象外とする理由は、①禁止しているのは自動生成物への継続的な蓄積であり、人が調査時点を明記して残す一回限りの実測記録は蓄積にあたらない ②本プロジェクトは「推論ではなく実測を根拠にする」ことを設計規律の中核に置いており、根拠を消すと後続 PR が同じ調査をやり直すことになる、の 2 点です。**ただしドキュメントに順位を書くときは必ず取得日を併記してください**（ランキングは 1 時間単位で変動するため、日付の無い順位は再現も検証もできません）。

Amazon ランキングには 2 種類のノイズがあるため、明示的に除去します。

- 「オンラインコード版」が同じゲームの別エントリとして重複している
- ゲームでない商品（PS ストアチケット、Nintendo Switch Online 利用券など）が混ざっている

> 📊 **2026-08-07 ライブ実測でノイズを定量化**（取得経路が今も健在であることも確認済み。50 件・`ranking` / `title` / `categoryName` 等のフィールドあり）
>
> | 項目 | 実測 |
> |---|---|
> | 非ゲーム商品 | **7 件**（ニンテンドープリペイド番号、PS ストアチケット、Roblox ギフトカード等）。**うち 1 位がプリペイド番号** |
> | 「オンラインコード版」を含む | 18 件 |
> | 同一タイトルの重複 | **5 組**（リズム天国 3 位と 8 位 / スプラトゥーン レイダース 5 位と 6 位 / トモダチコレクション 7 位と 15 位 / ファイアーエムブレム 11 位と 13 位 / Star Fox 44 位と 45 位） |
> | ノイズ除去後の実ゲーム数 | **38 件前後**（分母は 50 固定のまま） |
>
> 1 位が非ゲーム商品だったため、**ノイズ除去をしないと「Amazon 1 位 = 100 点」がプリペイド番号に付きます**。除去は必須です。重複の実例はいずれも「上位側を採る」規定で正しく処理できます。

> 📊 **2026-08-09 再実測**（PR #249 の実装時。取得経路そのものは §2.3 の制約どおり実装完了）。**2026-08-07 の実測から変動しており、ノイズの内訳は一定ではありません。**
>
> | 項目 | 実測 |
> |---|---|
> | 非ゲーム商品 | **9 件**（1 位がニンテンドープリペイド番号なのは変わらず） |
> | 「スプラトゥーン レイダース」の重複 | **4 位（オンラインコード版）と 6 位（-Switch2）**（2026-08-07 実測時は 5 位と 6 位） |
> | `categoryName` の使用可否 | **ノイズ判定に使えないと判明。** 実測で 5 種混在（ゲームソフト 38 / ダウンロード版ソフト/コンテンツ 5 / ジャンル別 3 / PCゲーム 2 / ハンドル・ジョイスティック 2）。**1 位のプリペイド番号が `categoryName=ゲームソフト`**、逆に本命の 4 位が `ダウンロード版ソフト/コンテンツ` だった。判定はタイトルベースで行った |
> | 追加で発見した非ゲーム類型 | **アップグレードパス / エキスパンションパス**（『ぽこ あ ポケモン エキスパンションパス』5 位、『ゼノブレイド2 …アップグレードパス』33 位）。別スナップショットでは **Game Pass Ultimate のサブスク商品**と**ガラスフィルム（保護フィルム）**も混入 |
> | ランキングの性質 | ⚠️ **1 時間単位のスナップショット**（`countingStartDate` / `countingEndDate` が 1 時間窓）。**実行のたびに順位が変わり、選定結果は run 間で再現しない。**検証中、ある取得では未掲載だった『ほの暮しの庭』が次の取得では 18 位に入った例を確認済み |
>
> キーワード方式のノイズ除去は本質的にスナップショット依存であり網羅は保証できません。ただし本実装は「母集団のゲーム側から索引を引く」方向（Amazon 側の全件を正規化するのではなく、IGDB の各候補から索引を引く）なので、除去漏れの非ゲーム商品は索引に残るだけで実害は出ません（fail-closed）。

**ノイズ除去後の順位の扱い**

- **順位は元のランキング順位をそのまま使い、除去で欠番が生じても詰め直しません。** 分母は掲載枠の **50 固定**です。
- **同一ゲームが複数エントリで重複している場合は、上位側の順位を採ります**（例: 3 位と 6 位が同じゲームなら 3 位）。
- **分母を 50 固定にする理由**: ファミ通経由のランキングは掲載枠が常に 50 件で、測定条件に依存しない定数です。Steam 軸だけが「実際の取得件数」を分母にするのは、そちらの取得件数が 9〜20 件と変動するためで、両軸の非対称はデータ源の性質の違いによるものです。
- **詰め直さない理由**: 除去は同一ゲームの二重計上と非ゲーム商品を排除するためのもので、他のゲームの順位を繰り上げる根拠にはなりません。Amazon 順位は「そのカテゴリでの売れ筋位置」という外部から与えられた絶対量として扱います。加えて、分母を週ごとのノイズ件数で動かすと同じゲームの点数が号をまたいでぶれ、絶対尺度を採用した利点（§2.3 の「重み付き最大値」の設計意図）を国内販売軸だけで失うことになります。

### 2.4 ▼ ロジック: 未発売側（不足分のみ）

**母集団の条件**

| 項目 | 値 |
|---|---|
| 発売日 | 日本時間当日 0 時 〜 90 日後 |
| ゲーム種別 | Main Game / リメイク / リマスター（✅ 2026-08-09、Issue #244・PR #249 で緩和。§6.2 が新作紹介でリメイク・リマスターを許可しているのに、未発売側の母集団だけ `Main Game のみ` のままで仕様矛盾があった） |
| テーマ除外 | 成人向けを除外 |
| 注目度 | 発売前フォロー数（hypes）が 20 超 |
| 発売日の精度 | **確定日のみ**（「2026 年 Q3」「2026 年 10 月」のような曖昧な日付は除外）。**取得経路は `games.date_format` ではなく `release_dates.date_format`**（実測で確認。§7.1） |
| 実在の裏付け | カバー画像がある／開発元が判明している |

⚠️ **#244 の緩和自体は仕様整合の修正として正しいが、実データでの観測可能な効果はゼロだった**（2026-08-09 実測）。`Rayman Legends Retold` は `game_type` を `(0)` → `(0,8,9)` に緩めても母集団に入らない。where 句合致は 33 件 → 34 件に増えるが、`sort first_release_date asc; limit 20` の先頭 20 件は緩和前後で完全に同一で、本作は 34 件中 23 番目（20 番目が `Aniimo` 2026-09-30、21 番目が `Neverway` 2026-10-01 で切れる）。**落としているのは `game_type` フィルタではなく `limit 20` と発売日昇順ソート。** limit の問題は Issue #250 に分離した → ✅ **完了**（2026-08-10）。`limit` を 20 → 50 に引き上げた。実測の母集団件数（33〜34 件）に十分な余裕を持たせ、発売済み側の2軸クエリ（Issue #241 / PR #243）が既に採用している `limit 50` と値を揃えた。

**並び順: 発売日の昇順（発売が近い順）**

注目度（hypes）は**ソート軸には使いません**。品質のフロア（下限）としてだけ使います。hypes 降順にすると 1 か月半先のタイトルが「もうすぐ発売」の先頭に来てしまうためです。

**「Steam ストアページの有無」を実在判定に使わない**

Switch 2 / PS5 専用の注目作（Marvel's Wolverine、Fire Emblem 新作など）は Steam に存在しません。「Steam にない = 実体がない」ではないため、実在判定は上記の 3 条件（確定日・カバー画像・開発元判明）で行います。

### 2.5 未発売タイトルの記事は構成を分ける

未発売のタイトルには**批評スコアもユーザー評価もレビューも存在しません**（実測: 未発売 973 件のうち批評スコアを持つものは 0 件）。それなのに現行のプロンプトは「レビュー情報を参考に特徴を書け」「どこが評価されているか書け」と指示していました。LLM は指示を満たそうとして推測を書くことになります。これを構造的に断ちます。

| 項目 | 発売済み | 未発売 |
|---|---|---|
| Web 検索（Tavily） | レビュー検索 + 開発者情報検索 | **レビュー検索を実行しない**。開発者情報検索 +「発売日・最新情報」検索 |
| 評価データの提示 | 批評スコアを提示 | **提示しない**（存在しない） |
| セクション 1 | 導入 | 導入 |
| セクション 2 | ✨ ゲームの特徴（レビューを根拠に） | **🔥 なぜ注目されているか**（公式発表・開発者コメント・シリーズの文脈を根拠に） |
| セクション 3 | 🎨 開発ストーリー | 同じ |
| セクション 4 | 👥 こんな人におすすめ | 同じ |
| セクション 5 | 📅 発売情報 | 📅 発売情報（**発売日と対応機種を明示**。確定日が保証されている） |
| セクション 6 | 🎯 Creator's Eye「どこが**評価されている**のか」 | 🎯 Creator's Eye「どこに**挑戦している**のか」 |

**セクション数は 6 のまま減らしません。** 6 セクションすべてについて、未発売タイトルでも材料が実在することを実測で確認しています。

**当日発売のタイトルは、本文はこの「未発売」列に従います**（レビューがまだ存在しないため）。ただしセクション 5「📅 発売情報」では**本日発売である旨を書きます**。発売日そのものは確定した事実なので、事実性の問題は生じません。見出しの扱いは §2.8 を参照してください。

### 2.6 ▼ ロジック: 未発売記事の Web 検索クエリ

| クエリ | 実行対象 |
|---|---|
| 「"タイトル" ゲーム 発売日 最新情報」 | 未発売のみ（新規） |
| 「"タイトル" 開発者 インタビュー / 開発秘話 / 開発ブログ」 | 発売済み・未発売の両方（既存のまま） |
| 「"タイトル" ゲーム レビュー 評価 感想」 | **発売済みのみ**（未発売では実行しない） |

**注意点として確認済みのこと**

- 検索クエリに **OR 演算子を使ってはいけません**。実測で 5 件中 2 件が完全に失敗しました（『Big Walk』の検索が映画『Big』の Rotten Tomatoes ページを引く、など）。OR を外すと両方とも正しい結果になりました。
- レビュー検索を未発売タイトルに実行すると、**「中身が空のレビュー集計ページ」と「前作のレビュー」**が高いスコアで返ってきます。これは「何も見つからない」より悪く、「レビュー」「点数」「評価」という語だけが裏付けなしに LLM に渡ります。vol.3 の『Forza Horizon 6』が未発売なのに「高く評価されています」と書いた原因はこれと考えられます。
- Tavily の呼び出し回数は増えません（レビュー検索 −1、新規クエリ +1 で ±0）。

### 2.7 ▼ ロジック: 未発売記事の事実性チェック

**評価断定の検出（新規）**

未発売タイトルの記事が「評価が高い」「好評」「絶賛」等と書いていたら、**high（重大）**の警告を出します。

| 項目 | 内容 |
|---|---|
| 発火条件 | 発売状況が「発売予定」**または「本日発売」**の記事（§2.8） |
| 検査対象 | 記事本文 **と** 要約（summary）の両方 |
| 検出語彙 | 評価が高い / 高く評価 / 高評価 / 好評 / 絶賛 / 支持を得 |
| 除外すべき文脈 | **「発売前の先行プレイで好評」は正当**。これを誤検出しない設計にする |

現在は「発売済み記事に『発売予定』と書いてある」ケースだけをチェックしていて、その逆（未発売記事が評価を断定する）のチェックが存在しませんでした。

**LLM-as-a-judge（事実性判定）の分岐**

判定用のシステムプロンプトは共通のままにし、**個別記事のメッセージに 1 行だけ追記**します。

> この記事は発売前のタイトルを扱っている。「評価が高い」「好評」「絶賛」等の受容に関する記述は、検索結果に発売前の先行プレイ評として明示されている場合を除き、`contradicted` または `unverifiable` と判定すること。

judge だけに任せない理由: judge のシステムプロンプトは「主観的表現・感想・期待感」を判定対象外と明記しています。「高く評価されています」はこの分類に入りうるため、上記の正規表現チェックが別途必要です。

### 2.8 ▼ ロジック: 発売状態は 3 値で扱う（当日発売の扱い）

**当日発売のタイトルは、本文と見出しで要請が逆になります。**

- **本文**: 記事が生成されるのは発行日の朝なので、ユーザーレビューも評価もまだ存在しません。書けるのは未発売用の構成だけです
- **見出し**: 読者が記事を読むのは日中以降です。「本日発売！」「ついに発売！」のような発売前提の見出しのほうが読者にとって自然です

この両立のため、発売状態を **2 値から 3 値に拡張**します。

| 状態 | 条件（日本時間） |
|---|---|
| `発売済み` | 発売日が当日 0 時より前 |
| **`本日発売`**（新規） | 発売日が発行日と同一 |
| `発売予定` | 発売日が翌日 0 時以降 |

**呼び出し元ごとに「本日発売」の解釈を変えます。**

| 経路 | 当日発売作の扱い | 理由 |
|---|---|---|
| 母集団クエリ（選定） | **未発売枠** | §2.2 のまま |
| 本文生成プロンプト・Web 検索セット（§2.5・§2.6） | **未発売** | レビューが存在しない。レビュー検索を実行せず、批評スコアも提示しない |
| 見出し生成プロンプト | **「本日発売」をそのまま渡す** | 「本日発売」「ついに発売」等の発売前提の見出しを書かせる |
| 評価断定バリデータ（§2.7） | **未発売** | レビューが存在しないので、評価の断定は検出対象 |
| 見出しの未発売表現バリデータ（既存） | **発売済み** | 「発売予定」「もうすぐ」等の誤表現は従来どおり検出する |

**なぜ 3 値にするのか**: 現在の `getReleaseStatus`（`bedrock-client.ts`）は 2 値を返し、見出し生成・本文生成・バリデータの 3 箇所で共用されています。当日発売作では見出し系と本文系で必要な値が逆になるため、2 値のままでは両立できません。3 値にすれば各呼び出し元が目的に応じて解釈を選べます。加えて見出しプロンプトには「発売済み」より「本日発売」と伝えたほうが、狙った見出しが出やすくなります。

**タイムゾーン: すべて日本時間（JST）基準に統一します。**

現在の `getReleaseStatus` はタイムゾーンを考慮せず、`YYYY-MM-DD` を UTC 0 時として解釈しています。§2.2 の境界は「日本時間の当日 0 時」なので、JST 基準に揃えます。境界を 1 日ずらす修正と、その境界がどのタイムゾーンかを決める修正を分けると、「JST では未発売だが UTC では発売済み」という 9 時間の穴が残るため、同じ PR で行います（PR-C）。

なお**発行日そのものの決定**が JST になっていない問題（§9.3 の 2）は別課題です。→ §9.3

---

## 3. インディーゲーム（2 本）

### 3.1 読者への約束

「小さなチームがどうやってこれを作ったのか」。読者がクリエイター志望である以上、**この枠が最も読者価値が高い**可能性があります。大手の新作は他メディアでも読めますが、小規模開発の話は探しにくいためです。

### 3.2 何を載せるか

- **発売済みのタイトルのみ**（未発売は新作紹介枠だけで扱う）
- **開発規模が小さいこと**が枠の条件
- **Steam で話題になっていること**（→ §3.5）。もともと「Steam / YouTube で話題」としていたが、YouTube 経路は実測で機能していなかったため 2026-08-07 に外した（活用の可能性は Issue #217 で別途検証）

### 3.3 未発売タイトルを扱わない理由

インディー記事には「💬 プレイヤーの声」（Steam レビューでの評判）というセクションがあります。これは未発売タイトルには**原理的に存在し得ません**。

実測すると、過去 17 号で発生した未発売記事は 2 件で、**どちらも新作紹介ではなくインディー枠**でした（vol.3 の『Forza Horizon 6』と vol.9 の『BrokenLore: FOLLOW』）。新作紹介側だけ対策しても、実際に起きた事故は 1 件も塞げません。

対策として「インディー側にも発売状態の分岐を入れる」のではなく「**インディー枠から未発売作を除外する**」を選びました。プレイヤーの声が存在しないタイトルは、そもそもこの枠の趣旨に合わないからです。

### 3.4 ▼ ロジック: 母集団と規模判定

**母集団の条件**

| 項目 | 値 |
|---|---|
| 発売日 | 過去 90 日以内、かつ日本時間当日 0 時**以前** |
| ゲーム種別 | Main Game のみ（リメイク・リマスターも除外） |
| テーマ除外 | 成人向けを除外 |

**規模判定: 「大手」を除外する**（✅ **実装済み**: 2026-08-09、PR #237）

以下のどちらかに当てはまれば「大手」としてインディー枠から除外します（OR 判定）。

| 判定 | 内容 |
|---|---|
| 開発本数による判定 | 開発元の **開発タイトル数が 20 を超える**なら大手 |
| 静的リストによる判定 | あらかじめ登録した大手企業リストに一致すれば大手 |

**なぜこの 2 本立てなのか**

- 静的リスト単独では、リストに載っていない会社がすべて「インディー」になります。これが Nihon Falcom（214 本）・Cygames（32 本）・Studio Wildcard（20 本）がインディー枠に載った原因です。
- 開発本数単独では、The Coalition（8 本）や Unknown Worlds（9 本）のような**大手の専属スタジオ**を拾えません。本数は少ないが資本的には大手です。
- したがって**両方を OR で併用することが必須**です。静的リストは廃止しません。

**閾値 20 の位置づけ**

| 大手側に落ちる（20 超） | インディー側に残る（20 以下） |
|---|---|
| Arc System Works (241) / Intelligent Systems (136) / Traveller's Tales (127) / Milestone (66) / Frogwares (44) / Supermassive Games (39) / Wargaming.net (34) / Frozenbyte (27) | One More Level (20, Ghostrunner) / Illfonic (19) / Mad Head Games (15) / Undercoders (14) / Yacht Club Games (12, Shovel Knight) / SUKEBAN (11, VA-11 HALL-A) / PocketPair (7, Palworld) / ZA/UM (6, Disco Elysium) |

Shovel Knight や VA-11 HALL-A は「小さなチームがどうやって作ったか」を語る枠に最も合う対象なので、これらを残す位置に閾値を置きました。20 と 27 の間に自然な切れ目があります。

**開発本数の数え方: 「生の件数」を使う**

開発本数には DLC・バンドル・移植・リメイクがすべて含まれます。これを「Main Game だけに数え直す」案は検討しましたが、**棄却しました**。

1. 数え直した値自体が不正確（Main Game にも「コレクターズエディション」などが混入している）
2. 誤判定の向きが悪い。DLC 主体の大手（Milestone は生 66 本 → Main 換算 2 本）を**インディー側に落としてしまう**。これは実際に起きた事故（大手がインディー枠に載る）そのものです
3. 閾値 20 では、数え方を変えても判定が変わる会社が 1 社もありません

**実装上の注意**

- 開発本数は IGDB のデータ更新で数件ずれます（Nintendo が 676 → 675 など）。閾値 20 はこのずれに影響されない位置ですが、境界値（20 / 21）のテストは必ず含めます。
- 閾値は環境変数化します。
- 開発本数は**既存の母集団クエリに `involved_companies.company.developed` を 1 フィールド足すだけで取れます**（2026-08-07 実測。既に `involved_companies.company.name` を選択しているため）。**企業情報エンドポイントへの別リクエストは不要**で、候補ごとにリクエストを絞る必要もありません。
  - 訂正の経緯: 本項はもともと「企業情報エンドポイントへの追加リクエストが必要。候補が確定した対象にだけ引く」と書いていました。これは `companies` エンドポイントを直接叩く方法だけを検証して書いたもので、より安い経路（`games` からの入れ子参照）を確認していませんでした。実測では `fields involved_companies.company.developed; where name = "Shovel Knight";` が HTTP 200 で Yacht Club Games = 12 本を返します（本仕様の値と一致）。
- **パブリッシャの出版本数は規模指標として使えません**（インディー系パブリッシャの方が本数が多い。tinyBuild 149 本に対し Yacht Club Games 17 本）。

### 3.5 ▼ ロジック: 話題性ルート（**YouTube 経路を外す**）

以下のいずれかを満たすものを「話題になっている」とみなします。

- Steam のおすすめ数が 5,000 以上
- Steam ランキング 200 位以内
- ~~YouTube トレンドの上位 30%~~ → **廃止**（2026-08-07 決定。下記）

**決定: YouTube 経路を話題性判定から外し、Steam の 2 経路だけにします**（ユーザー判断、2026-08-07）。実測で経路が機能していないことが判明したためです。**ただし YouTube を恒久的に捨てる決定ではありません** — 活用の可能性は別途検証します（→ Issue #217）。

> ⚠️ **2026-08-07 ライブ実測: このルートは 3 経路のうち 1 経路が壊れ、1 経路は健全でした。** もともと「現状維持」としていましたが、**見直しが必要**です。
>
> **(1) Steam おすすめ数は正常に取れます。** Storefront API の `recommendations.total` を実測: Stardew Valley 886,088 / Hollow Knight 497,737 / Lethal Company 410,941 / Hades 284,531。閾値 5,000 は機能します。
>
> **(2) 「YouTube トレンドの上位 30%」は実際には上位 80% を通しています（実在するバグ）。** 降順に並べた配列に対して `floor(件数 × (1 − 0.30))` の位置で閾値を取っているため、意図と逆になっています。数値で確認: 降順 10 件 `[1000, 900, …, 100]` で閾値インデックス 7 → 閾値 300 → **8 件通過 = 上位 80%**。
>
> なお**単純に `floor(件数 × 0.30)` に置き換えても正しくなりません**（n=10 で索引 3 → 閾値 700 → 4 件通過 = 上位 40%）。閾値を「上位 k 件の最小値」と定義するなら `k = max(1, ceil(n × 0.30))` を数え、その `k` 件目の値を閾値にするなど、**境界の定義を明示した上で境界値テストを書く必要があります**。Issue #217 で扱います。
>
> さらに、`youtubePopularity` を持たない候補が多数だと**閾値が 0 に落ちて事実上フリーパス**になります。品質フロアとして機能していません。
>
> **(3) YouTube 経路はそもそも候補をほぼ生み出していません。** トレンド動画 30 件 → タイトル抽出 20 件 → ユニーク 14 件 → **IGDB/Steam プール（113 件）にマッチしたのは 1 件だけ**で、13 件が破棄されました。破棄の内訳が問題の性質を示します。
>
> | 破棄の理由 | 実例 |
> |---|---|
> | 抽出がゲーム名でない | 「クリアレビュー」「モンスト公式」「ビャン」「オデット」「2026/08/05」「2026/08/06」 |
> | 表記が分裂して別キーになる | 「マイクラ」1,745,770 / 「マインクラフト」776,308 / 「マイクラ / マインクラフト」355,549 が**3 つの別エントリ** |
> | 日英表記でマッチしない | 「ビーストオブリンカネーション」（プールに英語名 `Beast of Reincarnation` が存在するのに未マッチ） |
>
> つまり YouTube 軸の欠陥は「マージ順序」だけではなく、**タイトル抽出の精度**と**日英・略称の照合**という 2 つの独立した問題です。§7.2 が新作紹介で YouTube を使わないと決めた判断は妥当でしたが、インディー枠はここで使い続けていました。
>
> **上記 (2) の閾値バグは、単独のバグ修正として切り出しません**（ユーザー判断、2026-08-07）。バグだけ直すと通過候補が減り、既に薄い供給をさらに狭めるためです。**廃止する経路のバグを直す意味もないため、経路ごと外します。**

**▼ 廃止の範囲（実読で確認した影響箇所）**

`youtubePopularity` は話題性フロア以外にも使われています。**外すのは話題性判定だけで、他の用途は今回変更しません。**

| 用途 | 扱い |
|---|---|
| インディー枠の話題性フロア（上位 30%） | **廃止する**。判定関数から YouTube 条件と percentile 計算を除去 |
| インディー枠の並び順（選定スコアの一項） | **廃止する**（→ §3.6 で Steam おすすめ数降順に置き換え） |
| 新作紹介の実存判定（`youtubePopularity > 0` を実存の根拠とする） | **今回は残す**。実存判定は「そのゲームが実在するか」の確認であり、話題性の精度とは別問題。ただし抽出精度の問題は同じく効くため Issue #217 の検証対象に含める |
| 記事生成プロンプトへの数値提示（「YouTube での累計視聴回数: N 回」） | **今回は残す**。ただし**分裂した表記の一部しか拾えていない値を LLM に渡している**状態なので（「マイクラ」だけで 174 万、「マインクラフト」77 万が別集計）、Issue #217 で扱う。なお本番 17・18 号の記事本文には視聴回数が出力されていないことを確認済み（漏洩はしていない） |
| データ取得そのもの（`fetchYouTubeData`） | **残す**。Issue #217 の検証に必要 |

> ✅ **実装しました**（2026-08-11。PR #273。マージ `d04a107`。Issue #235）。`meetsPopularityThreshold` から YouTube 分岐と `PopularityContext` を削除し、`fetch-data.ts` の `youtubePopularitySorted` 構築2箇所（通常選定・CompletenessGate 差し替え）を削除しました。

### 3.6 ▼ ロジック: 並び順

**Steam のおすすめ数の降順**（2026-08-07 決定）。

**現状の訂正**: 本節はもともと「現在は発売前フォロー数（hypes）の降順」と書いていましたが、これは不正確でした。hypes は **IGDB から 50 件取る段階**にしか効いておらず（`sort hypes desc`）、実際の掲載順は選定側の別のスコアで決まっていました。

```
現行の選定スコア = youtubePopularity + (1000 − steamRank) + igdbRating × 10
```

このスコアは使えません。第 1 項の YouTube は §3.5 で廃止したためです。

**タイブレーク**: おすすめ数を持たない候補は末尾に置き、その中では IGDB のユーザー評価点の降順とします。**キーが欠けた候補を配列順のまま放置しません。**

> ✅ **実装しました**（2026-08-09。PR #237。`compareIndieCandidates`）。旧スコア `computeIndieScore` は削除しました。
>
> ⚠️ **実装したところ、§3.4 の母集団条件「発売日 過去 90 日以内」が選定側に実装されていないことが表面化しました。** 旧スコアは Steam ランキング順位に強く依存していたため古い作品が上位に来ず、実害が出ていませんでした。おすすめ数の降順にした結果、**『Geometry Dash』（2013 年発売、おすすめ数 584,079）がインディー枠に採用されました**（実測で上位 10 候補中 5 件が窓外）。同 PR で発売日 90 日窓のフィルタ（`INDIE_RELEASE_WINDOW_DAYS`）を選定側に追加しています。**「並び順の変更」は「母集団条件の欠落」を表面化させる**、という一般則として記録しておきます。

**Steam おすすめ数を選んだ理由**

1. **実測で値が取れる**。Storefront API の `recommendations.total` で Stardew Valley 886,088 / Hollow Knight 497,737 / Lethal Company 410,941 / Hades 284,531
2. **§3.5 の話題性フロア（5,000 以上）と同じ値**なので、新しいデータ取得を追加しません
3. **「💬 プレイヤーの声」セクションの材料量と一致します**。おすすめ数が多いほど Steam レビューが豊富で、書ける内容が増えます
4. **名作深掘り枠と選定原理が重複しません**。評価母数の降順（§5.4）とは別の軸です

> ⚠️ **前提となる実装の欠陥（2026-08-07 のレビュー指摘を受けて実測で判明）。この修正なしでは並び順が機能しません。**
>
> **母集団クエリの変換処理が `websites` から `steamUrl` を抽出していません。** 抽出しているのは `mapRawGameToIGDBGame`（検索経路用）だけで、`fetchRecentPopularGames` / `fetchClassicGames` / `fetchIndieGames` の 3 つはいずれも `websites` を生のまま返します。そのため `fetch-data.ts:151` の appId 補完（`if (igdbGame.steamUrl)`）が**母集団由来の候補では一度も発火せず**、`steamAppId` が埋まりません。`steamRecommendations` は `steamAppId` を前提に取得されるため（`fetch-data.ts:431` の early continue）、連鎖して並び順キーが欠落します。
>
> **ただしデータは存在します。** 90 日窓の発売済み候補 33 件のうち、**30 件（91%）が `websites` に Steam ストア URL を持っていました**。真に Steam 非掲載なのは 3 件だけ（Rhythm Heaven Groove / Splatoon Raiders / Pokémon Infinite Fusion）。
>
> つまり「指標が取れない」のではなく「**取れるのに取っていない**」状態です。**3 つの母集団クエリの変換処理で `websites` → `steamUrl` を抽出する修正を PR-I に含めます**（→ §8）。
>
> ✅ **実装しました**（2026-08-09。PR #237）。実測での効果: `steamAppId` を持つ候補 **32 → 92 件**、うち IGDB 由来のみの候補では **0 → 59 件**、`steamRecommendations` を持つ候補 **17 → 60 件**。
>
> ⚠️ **実装時に、本ブロックが書いていない欠落がもう 1 つ見つかりました。** `aggregateGames` の IGDB 単独エントリ生成に `steamAppId` の設定が無く、IGDB enrich ループも `!coverImage || genres 空` のときしか走らないため、**`steamUrl` を抽出しただけでは Storefront 補完（`if (!game.steamAppId) continue`）に到達しません**。同 PR で `steamAppId` の引き継ぎも実装しています。
>
> ⚠️ **IGDB が `websites.category` を `websites.type` に改名していました**（2026-08-09 実測: 母集団 60 件で `category === 13` の一致は **0 件**）。Steam URL 抽出が機能していたのは URL 文字列の部分一致フォールバックのおかげでした。**同じ理由で `officialUrl` 抽出（`category === 1`）は全経路で機能していません** → **Issue #234**。
>
> ✅ **解消しました**（2026-08-09。PR #246。マージ `bd71e4f`）。`type` を主・`category` を後方互換とし、`IGDB_WEBSITE_TYPE` 定数に `website_types` の実測値域（1=Official Website / 13=Steam / 24=Nintendo ほか。7・20・21 は欠番）を記録しました。実測: `enrichGameWithIGDB` で 8 タイトル中 **5 件**が `officialUrl` を取得（修正前は構造的に 0 件）。ただし**母集団サンプル 50 件で `type === 1` を持つのは 14 件（28%）**にとどまるため、公式URLの主経路は引き続き Tavily です。
>
> ⚠️ **同 PR で、Issue #234 の影響表に無い3箇所目が見つかりました。** `fetchGameImageAndUrl`（特集記事用）は呼び出し元ゼロの死んだコードでしたが、**#117 で構造的欠陥として廃止したブロックリスト方式フォールバック**（非SNS・非ストアの先頭URLを機械採用＝`theminesa.studio` 事故の原因）を保持したままでした。修正ではなく削除しています。**死んだコードは「無害」ではなく、廃止済みの設計を温存する経路になり得る**、というのが本件の教訓です。

**棄却した案**

- **新作紹介の 4 軸スコアの流用**: 実データで計算した結果、**上位 10 件すべてが票数軸で勝ち、うち 8 件が 100 点で同点**になりました（500 票以上は一律 100 点にクリップされるため、3,595 票と 598 票が区別できない）。同点 8 件から 2 本を選ぶ基準が無く、実質「配列順」で決まってしまいます。加えて票数の多さは知名度と経過年数の関数なので「昔から有名な順」に寄り、名作枠と原理が重複します
- **Steam ランキング順位の昇順**: 実測で順位を持つ候補が少なく（Top Sellers 24 件 / Top Played 14 件）、インディーは 200 位以内に入りにくいため、大半の候補が並べられません

**既知の限界**: Steam に掲載されていないインディー（実測 33 件中 3 件）は、おすすめ数を持たないためタイブレーク側（IGDB 評価点の降順）で並びます。§3.2 の枠の条件が「Steam で話題」なので、この扱いは枠の定義と整合します。

### 3.7 未決事項

~~**規模判定（開発本数 20 超）の実装が未着手です。**~~ → ✅ **実装しました**（2026-08-09。PR #237）。静的リストと `developed > 20` の OR 併用、閾値は `LARGE_STUDIO_DEVELOPED_THRESHOLD` で環境変数化。

**残っている限界が 2 つあります**（いずれも実装時の実測で判明）。

| 限界 | 内容 | Issue |
|---|---|---|
| 会社レコードの重複 | IGDB に `Nippon Ichi Software`(developed=187) と `Nippon Ichi Software, Inc.`(developed=**3**) の 2 レコードがあり、ゲームによってどちらを参照するかが変わる。少ない方に紐づいたタイトルは規模判定が発火しない | **#236** |
| 多作な小規模スタジオ | 生件数を使うため、**Kairosoft（developed=88）**のような「多作だが小規模」なスタジオを大手と誤判定する。§3.4 が生件数を選んだ理由（DLC 主体の大手をインディー側に落とさない）は妥当だが、逆方向のケースが検討されていなかった | **#240** |

> ⚠️ **上表「会社レコードの重複」行の再現例は PR #276 で塞がれている。** PR #276（Issue #236 ②対応）が `Nippon Ichi Software` を静的リスト `LARGE_DEVELOPERS` に追加したため、この企業は `developed` 件数（3 件のレコードに紐づく場合でも）に関わらず静的リスト側で `hit: true` になり、この例では限界①を再現できなくなった。**限界①自体は未解決のまま**であり、①を検証する際は静的リストに無い別の企業で測る必要がある。

---

## 4. 特集（1 本）

### 4.1 読者への約束

「今この時期に意味を持つ切り口で、複数のゲームを横に並べて見る」。単体レビューでは見えない文脈を提供します。

### 4.2 何を載せるか

- **暦のイベント（記念日・季節行事）を起点にテーマを決める**
- そのテーマに合うゲームを 3 本以上紹介する

この枠は 4 カテゴリの中で唯一、当初の意図どおり機能していました。「世界保健デー特集：命を救うドクター・病院経営ゲーム」「国際博物館の日特集：古代遺跡の謎に挑む歴史探索ゲーム 4 選」「オカルト記念日に贈る、背筋が凍る本格ホラーゲーム 5 選」のように、記念日がゲームジャンルへの具体的な橋渡しになっています。17 号中 14 号でこれが成立していました。

### 4.3 変更点: イベントが 0 件の週の扱い

**壊れているのは「その週にイベントが 1 件もない」ケースだけです。** 2026 年は 52 週のうち 5 週が該当します。この週は固定文言（「今週の注目ゲーム特集」）にフォールバックしていました。

その結果どうなっていたか（実測）:

| | 紹介ゲームが他号の特集とも重複した割合 |
|---|---|
| イベント 0 件の号（2 号） | **50%**（10 タイトル中 5） |
| イベントがある号（15 号） | 10%（29 タイトル中 3） |

テーマが無いと、特集が「人気順の先頭を並べただけ」に収束します。vol.2 と vol.8 は 6 週間離れているのに、両方とも同じ 2 タイトル（Forza Horizon 6 / Slay the Spire II）を紹介していました。

**新しい挙動: イベントが 0 件の週は、発行日から過去方向に最大 7 日遡って直近の記念日を採用します。**

過ぎたばかりの記念日の方が、これから来る記念日よりずっと近いことが実測で分かっています。

| 0 件週 | 過去方向で最も近い記念日 | 未来方向で最も近い記念日 |
|---|---|---|
| 2026-04-11 | **1 日前** 駅弁の日（鉄道・旅ゲーム） | 11 日後 アースデイ |
| 2026-05-23 | **5 日前** 国際博物館の日 | 8 日後 世界禁煙デー |
| 2026-06-13 | **1 日前** 恋人の日 | 8 日後 父の日 |
| 2026-08-22 | **3 日前** 俳句の日 | 9 日後 野菜の日 |
| 2026-09-12 | **3 日前** 救急の日 | 8 日後 空の日 |

### 4.4 ▼ ロジック: イベント選択

| 項目 | 値 |
|---|---|
| イベント定義 | 日本の記念日・行事データ（127 件） |
| 通常の探索窓 | 発行日から **未来方向 7 日** |
| 0 件時の探索 | 発行日から **過去方向に最大 7 日**、最も近いものを採用 |
| 過去方向でも見つからない場合 | 8 日以降の未来方向を見る |
| 除外対象 | 直近 N 号が**実際にテーマとして使った**記念日（N は実装時に決定） |
| 最低紹介ゲーム数 | 3 本 |

**「窓に入った」と「テーマとして使った」を区別することが重要です。** 17 号の実測では、探索窓に入った記念日 40 種のうち、実際にテーマとして使われたのは 14 種だけでした。除外を「窓に入った記念日」まで広げると 8〜12 日前まで遡ることになり、「直近に限定する」という趣旨から外れます。除外は**実際に使った記念日のみ**に限定します。

**探索窓を常時広げる案は採りません。** 窓を 10 日にすれば 0 件週は消えますが、隣接する号で同じ記念日を使う危険が 51 週中 1 週 → 38 週に激増します。「0 件のときだけ遡る」段階的フォールバックである必要があります。

### 4.5 ▼ ロジック: ゲームの選び方（現状維持 + 修正 1 点）

特集は独自の母集団クエリを持ちません。候補は 2 経路から来ます。

1. 他の枠のために取得したゲームプールの残り → **他の枠の除外方針をそのまま継承する**
2. LLM がテーマから提案したタイトル → IGDB で実在を検証する

**修正点: 経路 2 の実在検証に除外フィルタが一切かかっていませんでした。** 成人向けタイトルも DLC もそのまま通っていました（実測: 成人向け 10 件中 7 件が通過、DLC は 2 件中 2 件が通過）。この経路の唯一の防御は登録 1 件だけのブロックリストという状態でした。→ 実在検証にも「Main Game のみ」「成人向け除外」を適用します（Issue #208）。

副作用として、上位のタイトルがフィルタで落ちると次の候補に繰り上がる現象がありますが、これは検索精度の既存の問題（『Portal 2』が『Portal Maze 2』にすり替わるなど）であり、この修正が新たに生む害ではありません。実測では過去号で使われた 12 タイトルのうち 11 件が修正前と同じ結果、変化した 1 件は改善でした。

### 4.6 構造化テーマは使いません

IGDB は「ホラー」「オープンワールド」「協力プレイ」のようなテーマ分類を機械可読で持っており、供給量も十分あることを確認しました（オープンワールド 283 件、ホラー 228 件、協力プレイ 451 件）。しかし**採用しません**。

理由: 記念日データに込められた「記念日 → ゲームジャンル」という編集意図から離れるため。「オープンワールド特集」は、「母の日に家族で楽しむ協力プレイゲーム」「七夕に遊びたい星空と宇宙のゲーム」のような季節性・情緒的な訴求を失います。

---

## 5. 名作深掘り（1 本）

### 5.1 読者への約束

「なぜこのゲームは何年経っても評価され続けるのか」。良いゲームの普遍的条件を学ぶ枠。**クリエイター志望の読者にとって最も学びが大きい枠**と考えられます。

### 5.2 現状の何が壊れていたか

vol.12 以降、**6 号連続で 2026 年発売の新作が「名作深掘り」として載っていました**（Dungeon Blitz R、Subnautica 2、MOLE、Pokémon Infinite Fusion）。

原因は 2 つです。

1. 母集団の条件が「発売前フォロー数が 100 超」だけで、**発売日の条件が一切なかった**。これは定義上「これから出る話題作リスト」です
2. 選定側の品質条件が IGDB ユーザー評価の点数だけを見ていて、**評価の母数（何人が評価したか）を見ていなかった**

### 5.3 何を載せるか

**「評価が高く、かつ評価が十分に定着している」タイトル。**

経過年数の条件は**設けません**。これは検討の途中で方針転換した点です。当初は「10 年以上前に発売」を推奨していましたが、それでは Elden Ring（2022 年）のような「評価が既に定着した新しい傑作」を構造的に落としてしまいます。

**経過年数は「評価が定着したか」の代理指標にすぎず、本来の指標は評価の母数です。**

実測でこれが裏付けられました。年数の下限を一切かけずに評価母数の降順で並べ、1 年分（52 本）を消費したときに含まれる「発売 3 年以内」のタイトルは **Baldur's Gate III の 1 件だけ**でした。新しい作品は自然に母数が薄いため（プール内の母数の中央値は 2019 年 559 → 2026 年 131）、降順ソートで後ろに回ります。年数の下限は不要な二重の網でした。

### 5.4 ▼ ロジック: 母集団

| 項目 | 値 |
|---|---|
| 総合評価（批評 + ユーザーの合成値） | **85 以上** |
| 評価母数 | **200 以上** |
| テーマ除外 | 成人向けを除外 |
| ゲーム種別 | Main Game、**または**「原作が母集団に存在しないリメイク・リマスター」 |
| 並び順 | 評価母数の降順 |
| 取得件数 | 200 件 |
| 経過年数の下限 | **なし** |
| 年代を意図的に散らす仕組み | **入れない** |

**母集団は 266 件**。52 週クールダウンで **5.1 年分**あり、供給に余裕があります。

> 📊 **2026-08-07 ライブ API 実測で再確認**: 同条件（`total_rating >= 85 & total_rating_count >= 200 & game_type = 0 & themes != (42)`）で **257 件**でした（Main Game のみ。下表の 254 件に対応する測定条件）。差の 3 件は IGDB のデータ更新による漂動で、供給の余裕という判断は変わりません。

> ✅ **実装済み（2026-08-09、PR #254）。** 着手前実測では `game_type=0` で 257 件、J-3-e 適用後 **268 件**（本節記載の 266 件とは IGDB 側の漂動範囲内）。**実際に候補になるのは `limit 200` により 200 件 = 3.8 年分であり、本節の供給根拠「266 件 = 5.1 年分」とは食い違う**（`limit 200` 自体は本節の決定事項どおりで、これは仕様と実装の記述上の食い違いの記録）。名作枠の実行結果は誤選定タイトル（`Splatoon Raiders`）から `The Witcher 3: Wild Hunt` に変化した。詳細は `docs/article-category-prompt.md` の名作枠PR「実施結果」節を参照。

**評価母数 200 を選んだ理由**

⚠️ **下表の件数はすべて「Main Game のみ」で測定した値**です（§5.5 のリメイク条件を適用する前の数字）。閾値の比較が目的なので測定条件を揃えてあります。**採用条件（200 + リメイク条件）での最終的な母集団は上記の 266 件**で、表の 254 件はリメイク・リマスターを含めていない分だけ少なくなっています。

| 閾値 | 母集団（Main Game のみ） | 52 週換算 | 落ちてしまう注目作 |
|---|---|---|---|
| 100 | 349 | 6.7 年分 | —（ただし 2026 年発売の母数の薄い作品が残ってしまう） |
| **200** | **254** | **4.9 年分** | 幻想水滸伝 II / Ghost of Yotei / Crimson Desert / Pragmata 等 |
| 300 | 200 | 3.8 年分 | + Half-Life: Alyx（VR の代表的名作） |
| 1000 | 90 | 1.7 年分 | + ティアーズ オブ ザ キングダム / Outer Wilds |

**母数閾値では排除できない 1 件**: `Resident Evil Requiem`（2026-02 発売、母数 303）は **200 でも 300 でも母集団に残ります**。ただし評価母数の降順では 200 位以下に沈むため、掲載には至らないと考えられます。確実に排除するには「発売から N か月」の最小条件が必要になりますが、それは §5.3 で棄却した経過年数条件の再導入にあたるため、**運用で観察してから判断します**（Issue #210 のパラメータ再調整に含める）。

**年代を散らす仕組みを入れない理由**: 1 年分 52 件の年代分布が 2010 年代 24 / 2000 年代 20 / 1990 年代 7 / 1980 年代 1 と自然にばらけるため、実装コストに見合いません。

### 5.5 ▼ ロジック: リメイク・リマスターの扱い

**「原作が母集団にいないリメイクだけを許可する」**という条件を使います。

| 許可するもの | 条件 |
|---|---|
| ① Main Game | 従来どおり |
| ② リメイク / リマスター | **原作が ① の集合に含まれていない**場合のみ |

**なぜ「Main Game のみ」では駄目なのか**

リメイク 22 件の原作を調べたところ、**11 件は原作も母集団の条件を満たしていませんでした**。

| リメイク | 原作が母集団に入らない理由 |
|---|---|
| Resident Evil 2 (2019) | 1998 年版の総合評価が 69 |
| Half-Life を原作とする Black Mesa | Half-Life の総合評価が 84（あと 1 点） |
| Resident Evil 4 (2023) | 2005 年版の総合評価が 84（あと 1 点） |
| Final Fantasy VII Remake | FF VII (1997) がゲーム種別「拡張版」扱いでプール外 |
| Tetris (1989) | 1985 年版は母数 15 件 |
| ポケットモンスター ファイアレッド | 赤・緑の総合評価が 80 |

つまり「Main Game のみ」にすると、**Resident Evil シリーズと Half-Life は名作深掘り枠に一度も登場できません**。

これは偶然ではなく構造的な偏りと考えられます。**2010 年以前の原作は、現代の基準（操作性・グラフィック）で評価されるため総合評価 85 を下回りやすい**のです。総合評価 85 という条件が古い名作の原作を体系的に落とし、リメイク版だけが基準を満たす方向に働きます。

**なぜ「リメイクを全部許可」では駄目なのか**

『The Last of Us』（2013）と『The Last of Us Remastered』（2014）と『The Last of Us Part I』（2022）が独立した 3 エントリとして母集団に存在します。再登場防止はタイトル文字列で照合するので、**同じゲームが 3 回「名作深掘り」に載り得ます**。

しかもこの混線は、タイトルの正規化（「Remastered」を削るなど）では原理的に解けません。

| 混線する組 | 版名の除去で直るか |
|---|---|
| The Last of Us / Remastered | 直る |
| Dark Souls / Remastered | 直る |
| The Last of Us / **Part I** | 「Part I」を削ると **Part II を原作と誤同一視する** ✗ |
| 時のオカリナ / **3D** | 「3D」を削ると **Super Mario 3D World を Super Mario World と誤同一視する** ✗ |
| ポケモン **ハートゴールド** / 金 | サフィックスではないので削れない ✗ |

「原作が母集団にいないリメイクだけ許可」なら、**原作とリメイクが同時に母集団に存在することが定義上あり得ないので、混線はゼロになります**。追加のデータ保持も履歴の移行も不要です。

編集ルールとしても自然に読めます: **「原作が母集団にあるならリメイクは不要。原作が入っていない作品だけ、リメイク版をその作品の代表として採用する」**

**移植（Port）はどの枠でも除外したままにします。** 実測 71 件のうち批評スコアを持つものは 1 件、ユーザー投票 15 以上は 0 件で、内容はレトロ復刻配信が大半でした。

> ✅ **実装済み（2026-08-09、PR #254）。** 本節の決着ブロック（論点J-3）は「`game_type=0` の ID 集合を作り `parent_game` がその集合に無い `t8`/`t9` だけを残す」という**ID 集合照合**を想定していたが、**着手前実測で `parent_game` はそのまま `parent_game.game_type`/`parent_game.total_rating`/`parent_game.total_rating_count` まで展開できることが判明し、1 ゲーム単位で自己完結的に判定できるようになった**（ID 集合の保持・照合が不要）。両方式が一致することは母集団の t8/t9 全 23 件で確認済み（不一致 0 件）。ただし初回実装は**親の `game_type` を見落として** `Final Fantasy VII Remake`（親 FF VII が `game_type=10` でプール外のため許可されるべき。本節上表に記載のとおり）を誤って除外するバグがあり、レビューで検出・修正した。`Black Mesa` は本節が採用可能としているとおり J-3-e を通るが、**`isFanGame()` に落とされる**（`keywords` に `fangame` を実測確認）。ファンメイド・リメイクの扱いは未決着のため引き続き仕様判断が必要。

### 5.6 ▼ ロジック: 「📜 ゲームの歴史」セクション

このセクションは**維持します**。ただし 2 点を修正します。

**問題だったこと**

プロンプトが「発売当時の背景、業界への影響など（150〜200 文字）」を要求している一方、同じプロンプトが「発売当時の業界状況、与えた影響に関する具体的な記述」を**禁止**していました。加えて個人名・売上本数・受賞歴・スコア・順位・続編の存在・開発期間もすべて禁止されており、歴史記述の材料がほぼ全部禁止対象でした。

出力の実測では、📜 セクションは 17 号中 16 号で実際に書かれており、**うち 9 号が禁止カテゴリに触れていました**。禁止事項を 1 つも踏まずに書けていたのは 7 号（44%）です。セクションを廃止すると、この 7 号分も同時に失います。

**修正 1: プロンプトの矛盾を解消する**

- 禁止リストから重複項目（「発売当時の業界状況、与えた影響に関する具体的な記述の禁止」）を削除する。同じプロンプト内の「推測や創作は絶対にしない」が既に同じことを述べており、この項目は念押しでありながら 📜 の要求と表面上正面衝突していました
- 代わりに 📜 の指示を「**提供された情報に無い歴史・影響は書かない。材料が無い場合はこのセクションを省略する**」に強める

**修正 2: プロンプトに渡す検索結果の抜粋を 300 字から 1,500 字に拡大する**

これが最も効果の大きい修正です。**律速は検索ではなく抜粋の長さでした。**

| 指標 | 実測 |
|---|---|
| そのゲーム専用のページを検索で引けた | **16 件中 11 件** |
| プロンプトに渡る 300 字が歴史記述の材料を含んでいた | **16 件中 7 件** |

差の 8 件は Wikipedia などの専用ページを引けているのに、**先頭 300 字が受賞リストや定型の序文で埋まっていた**ケースです。検索クエリを工夫しても、抜粋が 300 字のままではこの 8 件は救えません。

**さらに深刻な穴が同時に見つかりました。** プロンプトには 300 字しか渡していないのに、**バリデータは 1,500 字を見て「根拠あり」と判定していました**。

| 区間 | 出典が必要な定量値の個数 |
|---|---|
| プロンプトに渡る 300 字の中 | 10 個 |
| 300〜1,500 字にのみ存在 | **31 個** |

この 31 個は記事を書く LLM には渡っていないのに、バリデータは「検索結果に書いてある」として警告を抑制します。実際に『The Witcher 3』の日本語 Wikipedia は 300〜1,500 字の区間に「2800 万本」「5000 万本」「6000 万本」「6500 万本」を含んでいます。記事がこれを書いた場合、実体は内部知識由来（ハルシネーション）なのに「根拠あり」と判定されます。**抜粋を 1,500 字に揃えると、この穴は構造的に閉じます。**

Tavily の呼び出し回数は増えません（既に取得済みの本文を捨てずに使うだけ）。検索結果の本文は平均 1,591 字なので、1,500 字はほぼ全文に相当します。

**なぜ「300 字」と「1,500 字」が食い違っていたのか**（構造の確認）: プロンプト整形処理は 4 ブロック（レビュー情報 / 開発者情報 / Steam レビュー情報 / ゲームの歴史）でそれぞれ本文を 300 字に切っています。一方、記事データに保存される抜粋は別の上限（1,500 字）を使っており、バリデータはこちらに対して根拠を照合します。**この 2 つの上限が別々に定義されていることが穴の原因**なので、修正は 4 ブロックすべてを 1,500 字に揃えることになります（→ §8 の PR-E）。

**修正 3（補助）: 歴史検索のクエリに発売年を加える**

効果は不安定（10 件中 6 件で改善）なので主対策にはしませんが、併用する価値はあります。

> ✅ **修正1・3 は実装済み（2026-08-09、PR #254）。** `PromptTemplates.classicSystem` の禁止リストから重複項目を削除し、📜 の指示を強化した。`searchGameHistory`/`searchGameInfo` に発売年を省略可能な引数として追加し、`classic` カテゴリの呼び出しにのみ渡すようにした（他カテゴリの挙動は不変）。**修正2（抜粋 300→1,500字の統一）は本PRの対象外**（§8 の PR-E が担当）。
>
> ⚠️ **`/code-review` で本節が見落としていた欠陥が発覚した（→ #255）。** 本節は §2（📜ゲームの歴史）の禁止リストとの矛盾しか分析していないが、**同じ `classicSystem` の §5（Creator's Eye）にも「後世に影響を与えた革新的な要素」という影響記述の要求が残っており、同種の矛盾がある**。実効性・要否は仕様判断が必要なため別Issueに分離した。
>
> ✅ **決定（2026-08-10、本PR。Issue #255 はクローズ）。** Issue が提示した3案のうち、**Creator's Eye の「後世に影響を与えた革新的な要素」という要求項目自体を削除する**案を採用した。📜 セクションで導入した「情報が無ければ省略してよい」という同型のガードは、Creator's Eye が必須セクション（省略不可）であるため運用が複雑になる（省略可否の判断基準を新たに追加する必要がある）。要求自体を削れば、それに応えようとして発生するハルシネーションのリスクを構造的に避けられる。Creator's Eye に残る他の2項目（「このゲームが名作と呼ばれる理由をゲームデザインの観点から分析」「面白いゲームを作るためのヒントや学び」）は歴史的影響の断定を必要としないため、これらは維持した。

### 5.7 履歴の移行はしません

名作枠の条件が変わっても、**再登場防止の履歴には一切手を加えません。**

「誤って名作として載せた 2026 年の新作が 52 週ロックされる」ことを懸念していましたが、実測すると新条件で実際にブロックされるのは**既に掲載済みの正当な名作 6 件だけ**でした（Red Dead Redemption 2 は新母集団 5 位、Stardew Valley 36 位、The Last of Us Part II 38 位）。これは誤作動ではなく、クールダウンが重複掲載を防ぐという本来の目的を果たしている状態です。母集団 266 件に対し 6 件（2.3%）なので供給に影響しません。

一方、「誤選定分を削除する」案は**効果ゼロ**です。削除対象の 10 件は新しい母集団にそもそも含まれないため、履歴に残っていても選定を妨げません。得られるのは見た目の正しさだけで、履歴ファイルを手編集するリスク（過去に破損・履歴消失の事故がある）に見合いません。

「履歴を全リセットする」案は Red Dead Redemption 2 が即座に再選出され vol.2 と重複します。

### 5.8 ▼ ロジック: 並び順

**評価母数（total_rating_count）の降順**（2026-08-07 決定）。§5.4 の母集団表に記載のとおりです。

**4 軸スコアの流用を棄却した理由**

実データで計算すると、**上位 20 件が全件 100 点で同点**になり、順序を一切決められませんでした。

原因は構造的です。名作枠の母集団条件は「評価母数 200 以上」（§5.4）で、票数軸は「500 票で満点・以降クリップ」（§2.3）。**母集団の全候補が票数軸で満点に張り付きます**。Steam 軸と国内販売軸は数十年前の作品には効かないため、実質「批評軸か票数軸の最大値」になり、票数軸が常に 100 で勝ちます。

**評価母数の降順で得られる並び**（2026-08-07 実測）

```
5,890  GTA V (2013)                    3,783  Red Dead Redemption 2 (2018)
5,423  The Witcher 3 (2015)            3,547  The Last of Us (2013)
4,437  Portal 2 (2011)                 3,538  God of War (2018)
4,012  GTA: San Andreas (2004)         3,191  BioShock (2007)
```

年代が 2002〜2018 に自然に散っており、§5.4 の「年代を散らす仕組みを入れない」という判断が実データで裏付けられました。

**既知の限界**: 知名度の高い大作に寄ります。ただし §5.1 の約束（「なぜ何年経っても評価され続けるのか」）に対して、評価母数の多さは「評価が定着している」ことの直接の指標なので、枠の趣旨と整合します（§5.3）。

> ✅ **実装済み（2026-08-09、PR #254）。** `buildClassicCandidates` の並び順を `totalRatingCount` 降順に変更した。

---

## 6. 全枠に共通するルール

### 6.1 ▼ ロジック: 除外フィルタ

すべての母集団クエリに共通で適用します。

| 除外対象 | 判定方法 | 備考 |
|---|---|---|
| 成人向け | IGDB のテーマ ID **42（Erotic）** を除外 | **重要**: 以前は ID 37 を除外していたが、IGDB に ID 37 は存在せず**1 件も除外されていなかった**（修正済み） |
| DLC・拡張・バンドル・エディション違い | ゲーム種別を Main Game に限定 | 枠ごとに許可する種別を変えられるようパラメータ化する |
| 移植（Port） | 上記に含まれる（全枠で除外） | 品質条件をどの枠でも通らないことを実測確認 |
| ファンゲーム | タイトル・ジャンルに加え **IGDB のキーワード**（unofficial / fangame / fanmade）を見る | ✅ **実装済み**（2026-08-08。PR #230）。判定は**完全一致**。部分一致にすると `fan-translation`（有志翻訳がある**公式**ゲーム）/ `fanservice` / `fanfiction` 等の実在タグを誤検出する（ライブ実測で確認） |

> ⚠️ **除外フィルタは「母集団クエリ」ではなく「枠ごとの選定処理」にも掛かっている必要があります**（2026-08-08 に実データで発覚）。
>
> `isFanGame()` の呼び出しは新作枠とインディー枠にしか無く、**名作枠には掛かっていませんでした**。実測ではその名作枠に『Pokémon Infinite Fusion』（keywords に該当あり）が選ばれており、判定関数だけ直しても実害は消えない状態でした（PR #230 で名作枠にも適用）。**特集枠は✅ 完了**（2026-08-12。PR #288）。`generate-articles.ts` の `deduplicated`（経路1・経路2の合流点）への1箇所適用で、qualified / fringe の両分岐をカバーした。実測: qualified 227 → 225（−2。Black Mesa / Pokémon Infinite Fusion）。Issue #232 が当初対象としていた `fetch-data.ts:1316` の `featured` は特集記事に使われていないことが着手前検証で判明（→ Issue #289）。
>
> 同じ理由で、**IGDB のクエリ側で種別を制限しても枠の分離は保証されません**。`fetchIGDBData` が 3 つの母集団クエリの結果を 1 つのプールに平坦化してから返すため、ある枠のクエリにだけ許可した種別が他の枠の選定にも流れ込みます（§6.2 の脚注参照）。

**Steam 経路の DLC 除外（✅ 実装済み: 2026-08-08、PR #226）**

候補プールへの入口は IGDB だけではありません。Steam の Top Sellers / Top Played 経路には**DLC を除外する処理が存在しませんでした**。Steam API のレスポンスにある種別フィールド（game / dlc / demo / music）を読んでいなかったためです。

**現時点の実データで Top Sellers 10 件中 2 件が DLC** です。これが過去の DLC 混入事故（vol.13 の DJMAX DLC、vol.15 の GRANBLUE アップグレードキット、vol.17 の Grim Dawn 拡張）の真の原因と考えられます。IGDB 側のエディション混入は品質条件を通した層では 0 件（0.6〜1.0% で軽微）だったのに対し、Steam 経路は 20% です。

対応: **種別が game 以外を候補から除外します**（2026-08-07 決定 / 2026-08-08 実装）。親ゲームへの読み替えは行いません。

> 📊 **2026-08-07 ライブ実測**（Top Sellers 10 件を `appdetails` で検査）
>
> | 混入 | 種別 | 親ゲーム情報 |
> |---|---|---|
> | Street Fighter 6 - Year 4 アルティメットパス | `dlc` | **あり**（appid 1364780 "Street Fighter 6"） |
> | Big Walk and PEAK | 取得できず | なし |
>
> **10 件中 2 件**という仕様書の記述を再現しました。また **vol.18 の記事 1 本目が実際にこの Street Fighter 6 の DLC でした**（`issue-018.md:7`）。混入は現に起きています。
>
> **親ゲームに読み替えない理由**: `fullgame` フィールドが実在するので読み替えは技術的に可能ですが、**親ゲームは発売から年数が経っていることが多く（Street Fighter 6 は 2023 年発売）、結果として 60 日窓で落ちます**。読み替えても除外と同じ結果になるため、追加の API 呼び出しに見合いません。
>
> **受容する限界**: 「人気の DLC が出た」という事実自体は候補に反映されません。新作紹介は本体タイトルを扱う枠なので、これは枠の定義と整合します。

> 📊 **2026-08-08 ライブ実測（実装時）— DLC 以外の混入も確認**
>
> 実装前に再度ライブ API を叩いたところ、「Top Sellers 10 件中 2 件が DLC」は再現しました（`Street Fighter 6 - Year 4 アルティメットパス`（4 位）/ `キャラクターパス`（6 位）、いずれも `type=dlc`。Top Sellers は上位 20 件を処理するため 2 件とも候補処理に到達していました）。加えて**本節に記載の無かった混入経路**が見つかりました。
>
> | 混入 | 順位 | 種別 | 候補処理への到達 |
> |---|---|---|---|
> | `Pight Soundtrack`(4713630) | coming_soon **1 位** | **`music`（サウンドトラック）** | ✅ 到達 |
> | `Lord of Kensai Soundtrack`(4990990) | coming_soon **10 位** | **`music`** | ❌ 未到達 |
>
> ⚠️ **coming_soon は上位 5 件しか処理していません。** 生レスポンス 10 件のうち `music` は 2 件でしたが、**実際に候補プールへ入っていたのは 1 件**（`Pight Soundtrack`）です。
>
> `coming_soon` の取得結果は `fetchSteamData` 内で `topSellers` にマージされるため、DLC と同じ穴です。**種別が game 以外を除外する方針で同時に塞がります**（判定を「dlc の除外」ではなく「game 以外の除外」としたことが結果的に効きました）。

**実装時の挙動変更（受容・記録）**

種別が取得できない場合も除外側に倒れます。実装後は `appdetails` の取得自体に失敗した場合も同じ扱いになるため、**取得失敗時に候補が減ります**（実装前は Featured Categories 側の名前で採用されていました）。この点は Issue #227 に分離して記録してあります。なお実装前の挙動にも「`appdetails` が取れないと成人向け判定が `false` になり成人向けが素通りする」という別の穴がありました。

### 6.2 ▼ ロジック: リメイク・リマスターの枠別方針

| カテゴリ | 方針 |
|---|---|
| 新作紹介 | Main Game + リメイク + リマスターを許可し、**記事にその旨を明記させる**（✅ 2026-08-08 実装。PR #230） |
| インディー | Main Game のみ（影響 3 件と極小。DLC 混入防止という当初目的に合致）（✅ 実装済み） |
| 名作深掘り | Main Game + 原作が母集団にいないリメイク・リマスター（→ §5.5）。**現時点ではリメイク・リマスターを一律除外**（PR #230。条件付き許可は名作枠 PR で実装） |
| 特集 | `isFanGame` は✅ 適用済み（PR #288、2026-08-12）。**リメイク・リマスターは意図的に非適用**（継承元が3通りに割れて一意に定まらないため。ユーザー判断、2026-08-12） |

> ⚠️ **リメイク・リマスター非適用の理由**: §6.2 の「他の枠の方針を継承」は、継承元（新作=許可 / インディー=一律除外 / 名作=条件付き許可）が3通りに割れて一意に定まらない。特集はテーマに合うゲームを横に並べる枠であり、リメイクでもテーマ適合性は損なわれないため、非適用を許容する。

移植（Port）は全枠で除外します。

> ⚠️ **枠ごとの方針を IGDB クエリだけで表現してはいけません**（2026-08-08。PR #230 のレビューで判明）。
>
> `fetchIGDBData` は新作・名作・インディーの 3 つの母集団クエリの結果を**1 つの `igdbData.games` に平坦化**してから返し、`aggregateGames` はその単一プールを全枠の選定に渡します。したがって新作枠クエリにだけ `game_type = (0,8,9)` を許可しても、**そのリメイクは名作枠・インディー枠の選定にも候補として流れ込みます**。
>
> 実測: `Assassin's Creed Black Flag Resynced`（`game_type=8` / `igdbRating=85.24` / カバー画像・概要あり）は名作枠の候補条件を**すべて通過**しました。名作記事の生成にはゲーム種別を渡していないため、リメイクが原作の名作として種別の明示なく発行される状態でした。**選定側にも同じ条件を置くこと。**

**新作紹介で許可する理由**

一般のゲームメディアは『Halo: Campaign Evolved』『Star Fox』『アサシン クリード ブラック フラッグ RE:シンクロ』を当然「新作」として扱います。いずれも大手の主要リリースで、批評スコアも 81〜84 あります。数量的な影響は週 0.4 本と小さいものの、これらを機械的に除くのは読者の期待と乖離します。

**名作枠と結論が逆になる理由**

名作枠ではリメイクを無条件に許可すると、同じゲームが 3 回載る「混線」が 11 件発生します（§5.5）。新作紹介では**この問題が構造的に起きません**。名作枠の母集団は数十年分をカバーするため原作とリメイクが同居しますが、新作紹介の探索窓は **60 日**で、原作とリメイクが同じ 60 日間に発売されることは事実上ないためです。したがって新作枠では「原作が母集団にいないものだけ」という条件は不要で、単純に許可すれば済みます。

**「許可するだけ」では不十分な理由**

新作紹介のプロンプトは、禁止事項として**「続編・関連作・DLC・コラボの存在」**を挙げています。リメイクの原作は関連作にあたるため、提供データに書かれていない限り LLM は言及できません。結果として『Star Fox』(2026) が**完全新作の任天堂タイトルとして**書かれることになります。読者は誤認しますが、LLM 側はルールを守っているだけなので、バリデータも事実性判定も検出できません。

**明記させる方法**

上記の禁止リストは「**提供データに明示的に書かれていない限り**、絶対に記載しない」という形になっています。したがってゲーム種別を提供データ（記事生成時に渡す【ゲーム情報】欄）に載せた時点で、リメイクである旨の記述は禁止対象から自然に外れます。**禁止リストを緩める必要がありません**——他の項目にも穴を開けるリスクを避けられる点が、この方式の利点です。

実装は次の 2 つです。

1. ゲーム種別を IGDB のレスポンスから記事生成時のデータまで持ち回り、【ゲーム情報】欄に「種別: リメイク」等として提示する（Main Game のときは行を出さない）
2. プロンプトに 1 行追加する: 「提供データに種別がリメイク／リマスターと示されている場合は、記事本文でその旨を明記すること」

**未測定として認識しておくこと**

- ~~上記 3 タイトルが品質条件（**批評媒体数 2 以上**）を通るかは未測定です~~ → **一部測定できました**（2026-08-08。PR-B の実装時にライブ IGDB で実測）。『アサシン クリード ブラック フラッグ RE:シンクロ』は **`aggregated_rating_count = 4`** で品質条件を**通ります**（`aggregated_rating = 84` / `rating_count = 25`）。したがって (J-1-c) の効果は実在します。『Halo: Campaign Evolved』『Star Fox』は今回の母集団に現れなかったため未測定のままです
- 増分「週 0.4 本」の測定は 90 日窓で行われたもので、確定した窓は 60 日です。実際の増分はこれより少ないと考えられます
- 種別を提示したときに LLM が「原作の説明」に展開してしまう（＝禁止項目である原作のストーリー・内容に踏み込む）可能性があります。開発モードでの出力確認を PR の受け入れ条件にします

### 6.3 ▼ ロジック: 再登場防止（クールダウン）

| カテゴリ | クールダウン期間 |
|---|---|
| インディー | 35 週 |
| 新作紹介 | 17 週 |
| 特集 | 17 週 |
| 名作深掘り | 52 週 |

**号内でのカテゴリ間重複は禁止**します。同じ号で同一タイトルが新作紹介とインディーの両方に出ることは避けます。除外処理は既に存在しますが、比較がタイトルの完全一致なので**正規化したタイトルでの比較に変更**します（✅ 2026-08-08 実装。PR #230。4 箇所を `isAlreadySelected()` に集約）。

> ⚠️ **2026-08-08 訂正**: 本節はもともとこの変更の理由を「『Slay the Spire II』と『Slay the Spire 2』が別扱いになるため」と書いていましたが、**これは誤りです**。`normalizeTitle()`（`scripts/normalize.ts:19-27`）が吸収するのは**小文字化・全半角コロン・ハイフン・連続空白・`™®©`** の差だけで、**ローマ数字とアラビア数字の差は吸収しません**（実読で確認。実際に同日のクールダウン履歴に `slay the spire ii` と `slay the spire 2` が別キーで併存していました）。
>
> したがってこの変更が実際に吸収するのは大文字小文字・区切り文字の差異のみで、**現行のパイプラインでは挙動差分がありません**（比較対象はすべて同一の `games` 配列由来で、`title` と `normalizedTitle` は `aggregateGames` で同時に更新され、選定中には変化しないため）。比較の正しさをフィールドの同期に依存させない、という防御的な意味は残ります。
>
> 版名・数字表記のゆれは下記「既知の限界」に含まれる未解決問題です。

**同一タイトルの発売前 / 発売後の二重掲載は許可**します。未発売時に「もうすぐ発売」として扱ったタイトルを、発売後に改めて扱ってよいということです。記事内容が別物（発売前は期待、発売後は評価）なので読者価値があります。ただしこれには履歴に「発売前 / 発売後」の区別を持たせる改修が必要で、既存 105 件の移行を伴います（→ §7 の課題）。

**既知の限界（対策未定）**

- 履歴に日本語タイトルで記録されているものが 105 件中 20 件ある。『Subnautica 2』と『サブノーティカ２』が別キーになっている実害を確認済み
- クールダウンがカテゴリ別なので、同一作品が別カテゴリで重複掲載されている（実測 6 件）。これは実装どおりの挙動で、仕様として認めるかは未決
- 版名サフィックス（『Dark Souls』と『Dark Souls: Remastered』）が別キーになる。名作枠は §5.5 で母集団側から解決したが、新作枠・インディー枠には残る。**文字列の除去ルールでは原理的に解けない**

### 6.4 本数が足りなかったときの挙動

**公開は止めません。** 「枠を埋めるために不適格なゲームを載せない」「号全体を止めない」という設計原則はそのまま維持します。本数が足りなければ少ない記事数で発行します。

**変えるのは検証層です。** 本数不足を検証の警告として扱い、号のステータスを「エラー」にして **GitHub Issue の自動起票に乗せます**。

**何が問題だったか**

vol.17 の検証レポートは「記事 4 本」を**記録していました**。しかしステータスを決める判定は「重大警告の件数 / Web 検索の失敗 / 中程度の警告 / 公式 URL の欠落 / 事実性判定の件数」だけを見ていて、**記事本数は判定に一切入っていませんでした**。

つまり vol.17 で Issue が自動起票されたのは重大警告 3 件が理由で、**新作紹介 0 本は理由ではありませんでした**。重大警告が 0 件なら、新作 0 本でもステータスは「正常」になり、Issue も起票されず、GitHub Actions のサマリにも現れません。ログを開かないと気づけない状態でした。

**実測: 期待 6 本を満たしたのは 17 号中 14 号**

| 号 | 新作 | インディー | 特集 | 名作 | 計 |
|---|---|---|---|---|---|
| 001〜012, 014, 016 | 2 | 2 | 1 | 1 | 6 |
| **013** | 2 | 2 | 1 | **0** | **5** |
| **015** | **1** | 2 | 1 | 1 | **5** |
| **017** | **0** | 2 | 1 | 1 | **4** |

インディーと特集は 17 号すべてで充足しています。**vol.13 の名作記事 0 本は今回の調査で初めて判明しました**（原因は不明。当時のログが残っていない）。「気づけない」状態がどれだけ続いていたかを示す実例です。

### 6.5 ▼ ロジック: 本数不足の検出

| 項目 | 内容 |
|---|---|
| 判定条件 | カテゴリごとの期待本数（新作 2 / インディー 2 / 特集 1 / 名作 1）を **1 カテゴリでも下回れば**警告 |
| 重大度 | **high（重大）** |
| 波及先 | 号のステータスが「エラー」になり、既存の Issue 自動起票の経路に乗る |
| 生成層への変更 | **なし**（データ取得・記事生成・完全性ゲートには手を入れない） |
| その他 | サイト内の「毎号 6 本」という記述を実態に合う表現に直す（**`src/pages/launch.astro` に 5 箇所**。本文 3 箇所と、ティッカーの「6記事 / 号」2 箇所） |

重大度を high にしても毎週鳴るノイズにはなりません（実測で該当は 17 号中 3 号）。

**この変更で本数そのものは増えません。** 「不足に気づける状態にする」までが目的で、埋めるかどうかは運用実績を見てから別途判断します。

> ⚠️ **2026-08-07 訂正**: 本節はもともと「不足の原因（母集団の 75% が未発売 / 品質条件の経路が実質 1 本 / クールダウン）は新作枠・名作枠の再設計で既に手当てされている」と書いており、私は一度これをライブ実測で裏付けたと報告しました。**その裏付けは誤りでした**（3 つある母集団クエリのうち 1 本だけを測っていた。§2.2 の実測ブロック参照）。
>
> 実測では取得段階に 60 日窓の発売済みが 19 件入っており、**取得段階のボトルネックは存在しません**。したがって「再設計で手当て済み」は現時点で**証明されていません**。真の制約は下流（品質条件・実存判定・大手判定・クールダウン・完全性ゲート）にあると考えられますが、**どの段階で何件落ちるかは未測定です**。
>
> **本 PR（PR-G）の価値はむしろ上がりました。** 原因が特定できていない以上、「不足に気づける状態」を先に作ることが妥当です。原因の特定は §9.3-9 として切り出します。

---

## 7. 使う指標と使わない指標

### 7.1 ▼ 使う指標

| 指標 | 実体 | 主な用途 |
|---|---|---|
| **批評スコア**（aggregated_rating） | 外部批評媒体のスコア集計 = **Metacritic 相当** | 新作紹介のスコア軸、品質条件 |
| **批評媒体数**（aggregated_rating_count） | 集計に使われた媒体数 = スコアの信頼度 | 品質条件（2 以上）、批評軸の信頼度補正 |
| **総合評価**（total_rating） | 批評スコアとユーザー評価の合成値 | 名作枠の母集団条件（85 以上） |
| **総合評価母数**（total_rating_count） | 合成の母数 | 名作枠の母集団条件（200 以上）と並び順 |
| **ユーザー評価点**（rating） | IGDB サイト利用者の平均評価 | 参考 |
| **ユーザー投票数**（rating_count） | IGDB ユーザー評価の投票数 | 新作紹介のスコア軸・品質条件、インディー枠 |
| **発売前フォロー数**（hypes） | 発売前にフォローされた累計 | **未発売枠の品質フロアのみ**（20 超） |
| **開発本数**（developed） | 開発元が開発に関わったタイトル数 | インディー枠の規模判定（20 超が大手） |
| Steam Top Sellers 順位 | — | 新作紹介のスコア軸・品質条件 |
| Amazon 国内ランキング順位 | ファミ通経由 | 新作紹介のスコア軸・品質条件。**記事には出力しない** |
| ゲーム種別（game_type） | Main Game / DLC / リメイク等 | 除外フィルタ、**および新作紹介の記事へのリメイク明記**（§6.2） |
| テーマ（themes） | ホラー / Erotic 等 | 成人向け除外 |
| 発売日の精度（date_format） | 確定日 / 月のみ / 四半期 / TBD。**`games` 直下ではなく `release_dates.date_format`**（実測） | 未発売枠で確定日のみに限定 |
| 原作情報（parent_game） | リメイクの原作 ID | 名作枠のリメイク判定 |

**IGDB のドキュメントには誤記があります。** 投票数（rating_count）は「外部批評スコアの数」と説明されていますが、実体はユーザー投票数です（The Witcher 3 が 5,377 なので批評媒体数ではありえない）。本仕様は実測解釈を採ります。

**⚠️ 上表のうち現在すでに取得しているのは 4 つだけです**（実読で確認）。この表は「使うと決めた指標」の一覧であって、「取得済みの指標」の一覧ではありません。実装量の見積りを誤らせないため明記します。

| 状態 | 指標 |
|---|---|
| 取得済み（4 つ） | ユーザー評価点 / ユーザー投票数 / Steam Top Sellers 順位 / **Steam おすすめ数**（`recommendations.total`） |
| **未取得**（`games` クエリへのフィールド追加が必要） | 批評スコア / 批評媒体数 / 総合評価 / 総合評価母数 / ゲーム種別 / 発売日の精度 / 原作情報 / キーワード（ファンゲーム判定用） / **開発本数**（`involved_companies.company.developed`。既存クエリへの 1 フィールド追加で足りる） / **発売前フォロー数（hypes）** |
| **未取得**（取得経路そのものが未実装） | Amazon 国内ランキング順位 |

**発売前フォロー数（hypes）が「未取得」である理由**: `where` 句と `sort` 句では使われていますが（母集団の絞り込みと取得順の決定）、`IGDBGame` にも `GameData` にも hypes のフィールドが存在せず、**変換時に値が捨てられています**。§2.4 は未発売枠の品質フロアに hypes を使うと定めているので、そこでは値そのものが必要になります。

**Steam おすすめ数には前提となる修正があります**: 値は取得できますが、母集団由来の候補では `steamAppId` が埋まらないため実際には取れません（→ §3.6 の警告ブロック）。

テーマ（themes）は成人向け除外の `where` 句で使っていますが、値としては取得していません。

> ✅ **2026-08-07 ライブ API 実測: 未取得の 10 指標すべてについて、取得可能であることを確認しました**（「実装すれば取れる」ことの裏付け。仕様が実在しないフィールドに依存していないかの検証）。
>
> - `games` の 8 指標 — 1 クエリで同時取得できました（例: The Witcher 3 で `aggregated_rating` 91.73 / `aggregated_rating_count` 26 / `keywords` 配列）。ただし**発売日の精度は `games.date_format` ではなく `release_dates.date_format`** です
> - 開発本数 — `companies` エンドポイントでも引けますが、**`games` クエリの `involved_companies.company.developed` で取るほうが安く済みます**（HTTP 200 で Yacht Club Games = 12 本。既存クエリが `involved_companies.company.name` を選択済みなので 1 フィールド追加で足りる）。`companies` 直接では Nihon Falcom 214 / Arc System Works 241 / Yacht Club Games 12 / PocketPair 7 で、**検討資料の実測値と完全に一致**しており、閾値 20 の妥当性が再確認できました
> - Amazon 国内ランキング — ファミ通経由の取得経路が今も健在です（§2.3 の実測ブロック）
>
> **なお Issue #175 が挙げている `company.game_count` は存在しません**（クエリに含めると `400 Invalid field name`）。規模判定は `developed` の件数で行います（§8 の PR-I）。

### 7.2 ▼ 使わないと決めたもの

| 対象 | 理由 |
|---|---|
| **Metacritic / OpenCritic のスコア取得** | **17 号すべてで 1 件も取得できていなかった**。API キーがリポジトリ・ローカル環境・GitHub Actions のどこにも設定されておらず、常に空を返していた。IGDB の批評スコアが同等に機能するため復活させず、取得経路・型・選定条件・プロンプト参照・バリデータ参照を**削除する**（表示部分と過去号のスキーマは互換のため残す） |
| **発売前フォロー数（hypes）をソート軸に使うこと** | 定義上「発売前」の累計なので**発売後は増えない**。GTA V / Portal 2 / Skyrim / Half-Life 2 はいずれも 0。『Inside』は 1,617 票の名作なのに 11。発売済み作品の質を測る指標として本質的に不適合。**これが 3 カテゴリの乖離の根本原因** |
| **パブリッシャの出版本数（published）を規模指標にすること** | インディー系パブリッシャの方が本数が多い（tinyBuild 149 対 Yacht Club Games 17）。かつ他社開発作品を含む |
| **開発本数を「Main Game だけ」に数え直すこと** | 数え直した値も不正確で、DLC 主体の大手をインディー側に落とす（§3.4） |
| **IGDB の構造化テーマ（ホラー / 協力プレイ等）を特集テーマに使うこと** | 供給量は十分あるが、記念日起点という編集意図から離れる（§4.6） |
| **YouTube の注目度を新作紹介のスコア軸にすること** | 略称が 3 つのキーに分裂する（「スプラレイダース」146 万再生 / 「スプラトゥーン レイダース」103 万 / 「スプラレイダーズ」7.5 万）。加えてデータのマージ順序の問題で、IGDB に紐づかないタイトルは捨てられる |
| **Amazon の API（Creators API）を使うこと** | 「売れ筋 1〜50 位を列挙する」機能が存在しない（指定した商品の順位を返す機能しかない）。かつ日本ロケールのライセンスが 24 時間を超える保存を禁じている |
| **経過年数の下限を名作枠に設けること** | 評価母数の代理指標にすぎず、Elden Ring 型の「評価が定着した新しい傑作」を構造的に落とす（§5.3） |

---

## 8. 実装の進め方

実装は独立した PR に分割します。**カッコ内は依存関係**です。

| 順 | PR | 内容 |
|---|---|---|
| 1 | PR-0 | 特集の実在検証経路に除外フィルタを追加（Issue #208）— 最優先 — ✅ **完了**（2026-08-08。PR #219 + PR #220。Issue #208 はクローズ済み） |
| 2 | PR-0.5 | 除外フィルタを枠ごとにパラメータ化（挙動不変の純リファクタ。PR-A / PR-B / 名作枠 PR より前） — ✅ **完了**（2026-08-08。PR #224） |
| 3 | PR-A | Steam 経路の DLC 除外（他と独立） — ✅ **完了**（2026-08-08。PR #226。サウンドトラック `type=music` の混入も同時に解消） |
| 4 | PR-B | 新作紹介の 4 軸スコア実装（**ただし国内販売軸を除く 3 軸**。第 4 軸は PR-B2） + ファンゲーム検出の修正 + リメイク許可と記事への明記（§6.2）— ✅ **完了**（2026-08-08。PR #230）。着手前測定で**新作枠 0 本の原因を特定**（→ #231）。実装時に**名作枠に `isFanGame` が適用されていない**ことが実データで発覚し同 PR で修正。特集枠への適用は #232 に分離 → ✅ **完了**（2026-08-12、PR #288）。**レビューで「3 つの母集団クエリが 1 プールに平坦化されるため、クエリ側の種別制限は枠の分離を保証しない」ことが判明**し、名作枠・インディー枠の選定側にもリメイク除外を入れた |
| 5 | **PR-B2**（新規） | **国内販売軸（第 4 軸）のデータ取得経路を新設する。** §2.3 が 4 軸の 1 つに据えているのに、**取得実装がリポジトリに存在しない**（`famitsu.com` は公式 URL 判定のドメインリストにのみ登場。ランキング取得コードは無い）。作業は ①ファミ通 `/ranking/amazon` の `amazonRankingData`（50 件）を取得するスクリプトの新設 ②2 種のノイズ除去（オンラインコード版の重複 / 非ゲーム商品）③日本語商品名と英語 IGDB タイトルの照合（§9.2-4。**下位判断ではなくこの PR の主要作業**）④順位を永続化構造体に載せないこと（§2.3 の制約。`GameData` / `aggregated.json` / `selected-games.json` / frontmatter に出さない）。**PR-B の後**。ライセンス制約が設計を縛るため単独 PR にする ⚠️ **2026-08-09 追記（#238）: スコープ追加が必要**。①Amazon 掲載を `isQualifiedGame` の経路として通すこと（本 PR の ④「順位を永続化しない」制約と両立する設計が要る。`isQualifiedGame(g: GameData)` は `GameData` しか見ないため、掲載タイトルの集合を選定処理に渡す等の設計が必要）②「スプラトゥーン レイダース」↔「Splatoon Raiders」の照合成立を受け入れ条件に含めること。**ただし #241（母集団クエリの上限欠落）を先に直さないと、Amazon 軸を足しても候補プールに入らない** → ✅ **完了**（2026-08-09。PR #249。マージ `0a304eb`）。照合キーは `titleJa`（`alternative_names` は日本語をほぼ持たず不採用）。誤照合ガードは発売日差 365 日超。`isQualifiedGame` に批評媒体数・Amazon 掲載の 2 経路を接続し、新作枠にのみ有効化。レビューで判明した並び順からの順位漏れ（`newReleasesReserves` / 候補ログ）を本 PR で塞いだ。『Splatoon Raiders』は候補入りしたが 27 件中 4 位で不採用（受け入れ条件の後半は未達。重み再調整は Issue #210）。#244 は本 PR で仕様整合のみ対応し、実効果ゼロ（真因は `limit 20`）を **#250** に、批評経路の全枠波及を **#251** に分離した |
| 6 | PR-C | 未発売記事のプロンプト分岐 / 検索セット分岐 / 評価断定バリデータ / judge の追記 / インディー枠の発売状態フィルタ / **発売状態の 3 値化と JST 統一**（§2.8。`getReleaseStatus` を 3 値に拡張し、見出し生成・本文生成・バリデータの各呼び出し元で解釈を分ける） |
| 7 | PR-D | Metacritic / OpenCritic 経路の削除（削除のみ・挙動不変。名作枠 PR と直列に並べる） → ✅ **完了**（2026-08-10。単独の `refactor/remove-metacritic-path` ブランチとしてではなく、**PR #258（マージ `0af2a36`）に吸収されて実行された**）。`fetch-metacritic.ts`（260 行）を全削除し、`bedrock-client.ts` / `build-issue.ts` / `fetch-data.ts` / `types.ts` / `validate-article.ts` / `validate-existing-issue.ts` / `generate-articles.ts` / `game-filter.ts` / `select-newreleases-with-fallback.ts` / `.env.example`（`OPENCRITIC_API_KEY`）の `metascore`/`userScore` 参照も全除去。挙動不変（Metacritic 取得は API キー不備で全 17 号にわたり 0 件しか返しておらず、この経路は一度も発火していなかった）。詳細は `docs/article-category-prompt.md` の PR #258「実施結果」節を参照 |
| 8 | 名作枠 PR | 母集団条件の変更 + リメイク条件 + 📜 のプロンプト修正 + 検索クエリへの発売年追加。**`total_rating` / `total_rating_count` / `parent_game` はいずれも現在どのクエリでも取得していない**ため、PR-B と同型のフィールド追加・型追加・転記が必要 ⚠️ **2026-08-09 追記（#238）: 母集団クエリだけでなく選定側 `buildClassicCandidates`（`fetch-data.ts`）も評価母数ベースに変更すること。** 現在は `igdbRating >= 85` だけで通すため、他クエリ経由でプールに入った発売直後のタイトルを拾う（実測: 『Splatoon Raiders』は `total_rating_count = 7` で名作枠に選ばれていた）。PR-B の教訓（3 クエリの結果は 1 プールに平坦化されるためクエリ側の制限は枠を分離しない）と同型 → ⚠️ **2026-08-09 再確認（PR #249 の実行時）: 『Splatoon Raiders』が実際に名作枠に選ばれ続けていることを再確認した。** `buildClassicCandidates` が評価母数を見ずに `igdbRating >= 85` だけで通すという上記の指摘どおりの挙動が実データで再現している。選定側の修正は本 PR のスコープ外のまま、引き続きこの PR（名作枠 PR）の担当 → ✅ **完了**（2026-08-09。PR #254。マージ `0a2b025`）。母集団条件を `total_rating >= 85 & total_rating_count >= 200 & game_type=(0,8,9)` に、選定側 `buildClassicCandidates` も同条件（`meetsClassicPoolThresholds` + J-3-e）に一本化。実測: 名作枠選定が **Splatoon Raiders → The Witcher 3: Wild Hunt** に変化、他枠は不変。**着手前検証で `parent_game` が展開可能なことが判明**し、決着ブロックが前提とした ID 集合照合が不要になった（`igdbId` 追加も不要）。`/code-review` で選定側に `game_type` ゲートが無い欠陥を検出（実測39件。今回選ばれた Witcher 3 の GOTY版・拡張が混入候補だった）し同PRで修正。Creator's Eye の影響記述要求（#255）と特集プレフィルタのプロンプト肥大（#256）は仕様判断が必要なため分離。詳細は `docs/article-category-prompt.md` の名作枠PR「実施結果」節を参照 |
| 9 | **PR-I** | ✅ **完了**（2026-08-09。PR #237。マージ `7a2a0da`）。インディー枠の規模判定（`developed > 20` を静的リストと OR 併用）+ `steamUrl` 抽出の修正 + インディー枠の並び順（Steam おすすめ数降順）+ 発売日 90 日窓フィルタ。**着手後に「決着済みだが未実装」の論点A（新作紹介の企業規模ゲート撤廃、§11.1 確定事項 #1）を発見し、同 PR で実装した**。実測: 新作枠の採用 **0 本 → 2 本**、`steamAppId` を持つ候補 **32 → 92 件**。Issue #175 は `company.game_count` が IGDB に存在しないため `developed` 方式に読み替えて実装（`Refs`）。**本 PR の検証中に 7 件の Issue を分離**（#234 / #235 / #236 / #238 / #239 / #240 / #241） |
| 10 | PR-E | 検索結果の抜粋を 300 → 1,500 字に統一（**全カテゴリに影響**。PR-C と同じ箇所を触るためどちらかを先に入れてリベース）。**修正箇所は 1 つではなく 4 つ**: プロンプト整形処理の中でレビュー情報 / 開発者情報 / Steam レビュー情報 / ゲームの歴史の 4 ブロックがそれぞれ独立に 300 字で切っている |
| 11 | **PR-F0**（新規） | **発行日を JST 基準で決める**（§9.3-2）。原因は特定済みで、`PUBLISH_DATE` が `workflow_dispatch` の入力にしか渡らず schedule 実行では空になり、UTC 基準の日付＝金曜が入る。§9.3-2 自身が「PR-F の着手前に対応する」としているため、**PR-F の直前に置く**。イベント探索窓の当たり方が変わるので PR-F の測定前提になる |
| 12 | PR-F | 特集のイベント 0 件週フォールバック（**PR-F0 の後**。他とは独立） |
| 13 | PR-G | 本数不足の検出（PR-B・名作枠 PR より後に置くと、不足が実際に減ったかを観測できる） |
| 14 | **#241 対応** | ✅ **完了**（2026-08-09。PR #243。マージ `31770bc`）。新作枠の母集団クエリを**発売済み／未発売に分割**した。発売済み: `>= JST当日0時の60日前` かつ `< JST当日0時`、**2 本立て**（A: `hypes > 5` / `sort hypes desc`、B: `rating_count > 5` / `sort rating_count desc`、各 `limit 50`。和集合を id で重複除去）。未発売: `fetchUpcomingGames` を新設（`JST当日0時` 〜 `+90日`、`hypes > 20`、Main Game のみ、`sort first_release_date asc`、`limit 20`）。JST 当日 0 時は `getJstDayStartUnixSec`（§11.1 確定事項 #6 の算出式）に一元化。**実測: プール内の 60 日窓の発売済みが 4 → 73 件、未発売は 16 → 23 件で維持**。⚠️ **選定結果は変わらなかった**（下流の品質条件が Steam 依存のため。→ #238 の裏付け）。⚠️ **未発売のリメイク・リマスターがどちらのクエリにも入らなくなった**（§2.4「Main Game のみ」と §6.2「リメイク許可」の仕様矛盾。実測 1 件）→ **#244** → ✅ **#244 は PR #249（2026-08-09）で対応済み。** ただし §2.4 の `game_type` 緩和自体は仕様整合の修正として正しい一方、**実効果は実データでゼロだった**。`Rayman Legends Retold` は緩和後も `sort first_release_date asc; limit 20` の先頭 20 件に入らず（34 件中 23 番目）、**真因は `game_type` フィルタではなく `limit 20` と発売日昇順ソート**と判明。limit の問題は **#250** に分離した → ✅ **#250 は本PRで対応済み（2026-08-10）。`limit` を 20 → 50 に引き上げた**（詳細は §2.4 の該当パラグラフを参照） |
| **15**（新規） | **`isQualifiedGame` の再設計** | `article-category-spec-review.md` §6.2 に論点として存在するが、**本表のどの PR にも割り当てられていない**（2026-08-09 に発覚。論点A と同じ構図）。現行は実質 `igdbRatingCount >= 15` と `steamRank` の 2 経路しか生きておらず、§2.3 の品質条件（批評媒体数 2 以上 / Amazon 掲載）が実装に存在しない。**§6.2 の素案（`aggregated_rating` 経路の追加）だけでは #238 は解決しない**（『Splatoon Raiders』は批評媒体数 1）。論点D の結論と合わせて PR を割り当てること → ✅ **完了**（2026-08-09。PR #249。PR-B2 に同梱）。批評媒体数 2 以上（`aggregated_rating_count`）と Amazon 掲載の 2 経路を追加し、分岐は 5 → 7 になった（本行が「6 経路」としていたのは記載時点で既に誤りで、実際は 5 経路だった）。**§2.3 に規定が無いのに残した経路は 3 つ**: `metascore`（削除は PR-D の担当。論点D は未決着）/ `steamPlayers > 0`（§2.3 が挙げるのは Top Sellers であって Top Played ではない。**担当 PR が未割り当て**）/ `igdbRating >= 85 && rc >= 8` の救済。⚠️ **同 PR で `hasExistenceEvidence` にも Amazon 経路を足した**（品質条件だけ通しても実存フィルタで落ちる構造が残るため。§2.4 の「カバー画像／開発元」とは別レイヤーの短絡） → ✅ **この 3 経路は PR #258（2026-08-10、マージ `0af2a36`）で全て解消した。** ①`metascore` 経路: **削除**。論点D（D-1'、決着済み・PR-D 未着手）の担当作業を PR #258 が実行した。`fetch-metacritic.ts` を全削除し、挙動不変（全 17 号で 0 件しか返していなかったため）。②`steamPlayers > 0` 経路: **削除**。この経路は「未仕様」なだけでなく、**恒常的なデッドコードだったことが新たに判明した**——`fetch-steam.ts` は `SteamData` に `peakPlayers` しか設定せず、`fetch-data.ts` の `aggregateGames` は存在しないフィールド `currentPlayers` を読んで `GameData.steamPlayers` に代入していたため、本番で `steamPlayers` は常に `undefined` だった。削除は挙動不変。③`igdbRating >= 85 && rc >= 8` の救済経路: **削除せず維持**し、§2.3 の品質条件表に条件2の閾値緩和版として明文化した（管理者判断・ユーザー確認済み）。実データで機能している実在の経路であることを確認済み。詳細は `docs/article-category-prompt.md` の PR #258「実施結果」節を参照 |

**この表に含めていない小改修**（どこかの PR に相乗りさせる。単独 PR にはしない）

- 号内カテゴリ間重複の比較を、タイトル完全一致から正規化タイトルに変更する（§6.3）。触るのは選定処理の 2 箇所。PR-B に相乗りさせるのが自然

**検証方法**

- **着手前に、その PR が依存する外部データ源をライブ API で実測する。** 2026-08-07 の調査で、仕様が依存する YouTube 経路の破綻と新作枠の供給不足が「実装前に」判明した。逆に、古い `data/aggregated.json`（中身は 2026-05-16 で、取得コードより 1 か月古い）を根拠に「フィールドが 0 件だから機能していない」と誤判定した事例もある。**中間データファイルを実測の代わりに使わないこと**
- 実装前に失敗するテストを書く
- 閾値は環境変数化し、本番コードにテスト用の分岐を入れない
- 境界値（閾値ちょうど、順位ちょうど）のテストを必ず含める
- 実データ検証は `DEV_MODE=true npm run build-issue:dev`（`issues-dev/` に出力。本番ディレクトリには書かない）
- 変更前後で選定結果を比較し、意図しない除外が起きていないか確認する
- 回帰ケースを追加（成人向けのすり抜け、DLC 混入、Nihon Falcom のインディー誤判定）
- PR-E は抜粋が長くなることで出力の焦点がぼやける可能性があるため、4 カテゴリすべての出力を確認してからマージする

---

## 9. 未決事項・保留事項

### 9.1 保留（ユーザー判断により先に別の検討が必要）

| # | 項目 | 状況 |
|---|---|---|
| 1 | **重大（high）警告の重大性の再定義** | 過去 28 レポートの high 警告 96 件を分類すると、性質が一様でない。「対応機種の不一致」31 件と「本文のタイトル不一致」11 件は**プロンプトで明示的に禁止しているのに守られていない**もの（プロンプト遵守の失敗）。一方で最大 17 件は**検索結果に根拠がある数値を転記したケース**で、バリデータが「ゲーム情報欄」しか正解と認めていないことによる誤検出の可能性がある。「high は記事を公開してはいけないほどのものか」を先に検討する |
| 2 | **記事の自動再生成の適用範囲** | 上記 1 の結論に依存するため保留。現在はデフォルト OFF のまま。全体を ON にすると生成コストが約 2 倍（high が 0 件の号は 28 件中 4 件のみ） |

### 9.2 実装時に決める下位判断

| # | 項目 |
|---|---|
| 1 | ~~インディー枠の並び順~~ → ✅ **決着**（2026-08-07。**Steam おすすめ数の降順**。4 軸スコアの流用は実データ計算で「上位 8 件が 100 点で同点」になるため棄却。§3.6） |
| 2 | ~~名作枠の並び順~~ → ✅ **決着**（2026-08-07。**評価母数の降順**。4 軸スコアの流用は実データで上位 20 件が全件 100 点の同点になるため棄却。§5.8） |
| 3 | ~~Steam 経路の DLC を捨てるか、親ゲームに読み替えるか~~ → ✅ **決着**（2026-08-07。**捨てる**。親は 60 日窓外で落ちるため読み替えても同じ結果。§6.1） |
| 4 | ~~Amazon ランキングの商品名と IGDB タイトルの照合方法~~ → **下位判断ではなく PR-B2 の主要作業に格上げした**（§8）。日本語の商品名と英語の IGDB タイトルを突き合わせる必要がある。§6.3 の「履歴に日本語タイトルで記録されたものが 105 件中 20 件あり別キーになる」問題と同じ構造で、既知の弱点。現在は `normalizeTitle` による集約で偶然吸収されている状態 → ✅ **決着**（2026-08-09。PR #249）。**IGDB `game_localizations` の `region === 3` にある `titleJa` を照合キーにする。** `alternative_names` は実測で日本語をほぼ持たない（発売済み 60 日窓の母集団 17 件中 CJK 別名は 3 件で全て中国語）ため使えないと判明した。誤照合ガードは**発売日 365 日超の差**（正しい 6 ペアはすべて 0〜1 日差だった） |
| 5 | 開発元が不明な候補の扱い（実測 11 件中 1 件）。インディー枠の規模判定ができない（PR-I で決める） |
| 6 | 資本関係（親会社）を規模判定の第 3 のシグナルとして併用するか（保有率 15%、親会社が多段になっている） |
| 7 | 新作紹介とインディーで候補が重複する（11 件中 6 件）。どちらを先に確定させるか。**現状は新作紹介が先に確定し、インディー側が採用済みタイトルを除外する順序で実装済み**（実読で確認）。変更する積極的な理由がなければ現状維持でよい |
| 8 | 特集で使った記念日の記録先（現在の履歴はゲームタイトルのみを記録しており記念日名を記録していない）と、除外する号数 |
| 9 | 特集のフォールバックが発火したことをログまたは記事データに残す方法 |
| 10 | 本数の期待値をどこに置くか（環境変数か定数か） |
| 11 | ~~新作紹介の品質条件を緩めるか~~ → **判断不要**（2026-08-07）。当初「供給 16 件では 2 本埋まらない」と考えたが、**未発売枠のフォールバック（§2.2）を勘案していなかった**。採用可能な合計は 16 + 29 = 45 件で供給は足りている。ただし**不足の真の原因は未特定**なので、下流の通過率の測定を §9.3-9 として切り出した |
| 12 | ~~§3.5 話題性ルートの見直し~~ → ✅ **決着**（2026-08-07。YouTube 経路を廃止し Steam 2 経路にする。§3.5）。YouTube 活用の可能性の検証は **Issue #217** として分離 |

### 9.3 別課題として切り出したもの

| # | 項目 |
|---|---|
| 1 | **受賞歴・ランキング順位・裸の人名を検出するバリデータが存在しない**。「受賞」「Game of the Year」「第 N 位」を捕まえるパターンが 1 つもなく、人名の検出は「〜氏によると」「CEO の〜」形式しか見ないため『Eric Barone』のような裸の人名を検出しない。📜 セクションで実際に漏れているのはまさにこのカテゴリ（受賞歴 3 号 / 個人名 2 号）。全カテゴリに効く問題なので上記 9.1 の 1 と同じ束で扱う |
| 2 | **発行日が土曜でなく金曜になっている**（実測 17 号中 11 号が金曜）。**原因は特定済み**（2026-08-06、コード実読）: cron は `0 21 * * 5`（金曜 UTC 21:00 = 土曜 JST 6:00）で正しく、`PUBLISH_DATE` は `workflow_dispatch` の `inputs.publish_date` にしか渡っていないため **schedule 実行では空**になる。その場合 `generate-articles.ts` の `new Date().toISOString().split('T')[0]` が **UTC 基準の日付 = 金曜**を返す。修正は発行日を JST 基準で決めること（例: ワークフローで JST の日付を `PUBLISH_DATE` に渡す、または生成側で JST に変換する）。特集のイベント探索窓の当たり方に直接影響するため PR-F の着手前に対応する（**§8 の PR-F0 として順序表に組み込み済み**）。なお §2.8 の JST 統一（PR-C）は「発売日と発行日の比較」を JST に揃えるもので、こちらの「発行日そのものの決定」とはレイヤーが違う |
| 3 | **履歴に「発売前 / 発売後」の区別がない**。同一タイトルの二重掲載を正しく許可するには履歴の構造変更と既存 105 件の移行が必要 |
| 4 | **vol.13 の名作記事が 0 本になった原因が不明**。今回の調査で初めて判明。当時のログが残っていないため確認できない。本数不足の検出（PR-G）を入れれば今後は Issue として上がる |
| 5 | **過去号の生成ログ・集約データが残っていない**。「不足した」のか「そもそも生成しようとしなかった」のかを事後に区別できない。集約データを Actions のアーティファクトとして保存する仕組みが必要 |
| 6 | **本番の検証レポートが 1 件しかない**（vol.17 のみ。手動実行分が 7 件、開発用が 21 件）。継続保存が始まったのは Issue #202 以降 |
| 7 | **vol.2 の記事に「Metacritic で 89 点」という記述がある**。Metacritic のスコアは常に取得できていなかったので、これは提供データに基づかない数値と考えられる。現在のバリデータが「提供データに無い出典付きの数値」を捕まえられるかは別途検証が必要 |
| 8 | **エディション違い（Deluxe Edition 等）は Main Game として登録されているため除外しきれない**。ただし品質条件を通した層では実測 0 件なので優先度は低い |
| 9 | ~~**新作紹介の記事本数が不足する真の原因が未特定**~~ → ✅ **特定した**（2026-08-08。PR-B の着手前測定。→ **Issue #231**）。実測: 候補 23 件 → `finalizeGameMetadata` 失敗 5 件 → **大手判定ゲート不通過 18 件** → 採用 **0 件**。ただし**18 件のうち 16 件は正しく除外**されており（PocketPair 等）、**候補にあった大手タイトルは 2 件だけで、その 2 件が両方とも判定漏れで落ちていた**（`Arc System Works`/**Sony Interactive Entertainment**、`Nippon Ichi Software`/**NIS America**。いずれも `isLargeStudio` の静的リストに未登録）。**判定さえ正しければ期待どおり 2 本埋まっていた。** 対応は静的リストへの追記ではなく **PR-I**（`developed > 20` を静的リストと OR 併用）で構造的に行う（`isLargeStudio` は新作枠ゲートとインディー判定の共通関数のため）。これにより **PR-I を PR-B2 より前倒しする**（2026-08-08 のユーザー判断） **⚠️ 2026-08-09 追記: この診断（および Issue #231 の対処方針）は誤りだった。** 真の原因は「静的リストの不備」ではなく、**論点A（新作枠の企業規模ゲート撤廃、§11.1 確定事項 #1）が決着済みなのに実装されていなかったこと**。PR #237 でゲートを撤廃し、新作枠の採用は **0 本 → 2 本**になった |
| 10 | **決着済みだが PR に割り当てられていない決定が他にもある。** 2026-08-09 時点で判明していたもの: ①`isQualifiedGame` の再設計（spec-review §6.2。→ §8 の 15 行目に追加）→ ✅ **完了**（2026-08-09。PR #249。PR-B2 に同梱）②§3.5 の YouTube 話題性ルートの廃止（→ **Issue #235**）→ ✅ **完了**（2026-08-11。PR #273。マージ `d04a107`）。**2026-08-11 時点で①②とも解消済み。** ただし**この種の取りこぼしを構造的に防ぐ手段は無いままなので、§11.1 の確定事項の表と §8 の PR 一覧を定期的に突き合わせること** |
| 11 | **新作枠の母集団クエリに発売日の上限が無く、未発売の大作が枠を占領している**（→ **Issue #241**）。実測で、Amazon 国内ランキング上位の国内タイトルであっても候補プールに入らない経路がある。**品質条件やスコア軸を整備しても、その手前のプールに入らなければ対象にならない** |
| 12 | **IGDB のフィールド改名を検知する仕組みが無い。** `websites.category` → `websites.type` の改名（→ **Issue #234**）は、**指定しても 400 にならず単にレスポンスから消える**ため、静かに機能が死ぬ。同種の破綻を早期に検知する手段（取得件数の監視等）が無い。**⚠️ #234 自体は PR #246 で解消したが、この「検知手段が無い」という保留事項は未解消のまま**。#234 が発覚したのも別PR（PR-I）の着手前調査という偶然によるもので、仕組みで捕まえたわけではない |
| 13 | **特集記事の `recommendedGames[].officialUrl` が信頼済みソース判定にも到達性チェックにも一切かからずに出力されている**（→ **Issue #247**）。特集記事は `sourceUrls` を持たないため `build-issue.ts` のゲートに構造的に到達しない。実測（2026-08-09）: 発行済みの号にある 38 のユニーク URL のうち **5 件が公式サイトではない**（Bluesky プロフィール4件・Discord 招待1件）。PR #246 のレビューで発覚したが、**#246 以前から存在する欠陥** → ✅ **完了（2026-08-10。PR #269。マージ `7cfa916`）**。着手前の独立検証で、Issue本文の診断より深い根本原因を特定した: 漏洩したURLの一部（`Slay the Spire II`等）は**IGDB側に正しい公式URLが存在していた**にもかかわらず出力されていなかった。原因は`fetch-official-jp-url.ts`の`NON_OFFICIAL_URL_PATTERNS`に`bsky.app`/`discordapp.com`が抜けており、Tavily経由の誤った候補がIGDBの正しいURLを無条件に上書きしていたため。**根本原因の修正**（ドメインブロックリストへの追加。全カテゴリに波及）と、**Issue本文が求める多層防御**（`recommendedGames[].officialUrl`への出力時ゲート追加）の両方を実施した。詳細は`docs/article-category-prompt.md`のPR #269「実施結果」節を参照 |
| 14 | **§2.3「批評媒体数が 2 以上」に評価点の下限を設けるべきか**（→ **Issue #251**。PR-B2 のレビューで分離）。現状の条件は媒体数だけを見るため、低評価の批評が 2 件あるだけで通過できてしまう。Issue の元測定（2026-08-09）は「146 件の母集団中、批評経路のみで通過する 8 件はいずれも評価 74〜87」としていたが、**母集団の分母 146 件はどう計算したか含めて再現できなかった**（`buildNewReleaseCandidates` の母集団定義で追ってみたが 106 件にしかならず、元測定の絞り込み手順は残っていない → **確認できない**）。一方で**批評経路のみ通過するタイトルの内訳（銘柄・件数・評価点）は 2 回とも独立に再現できた**: 2026-08-09 時点のスナップショット（149 件中、母集団 106 件）で 8 件（Echoes of Aincrad 74/3件、Zero Parades: For Dead Spies 76/2件、The Adventures of Elliot: The Millennium Tales 79/4件、Avatar Legends: The Fighting Game 81/3件、Star Fox 82/2件、Denshattack! 84/4件、Realm of Ink 84/2件、Luna Abyss 87/2件、いずれも銘柄・評価点が元測定と一致）、2026-08-10 の再測定（303 件中、90 日窓の母集団 101 件 / 60 日窓 85 件）で 90 日窓 9 件・60 日窓 6 件（評価 74〜86.5）。**いずれの回でも評価 60 未満の批評経路のみ通過タイトルは 0 件。** ✅ **決定（2026-08-10。監視項目として保留、コード変更なし）: 現時点では §2.3 に評価点の下限を追加しない。** 懸念自体は原理的にあり得るが、2 回の独立測定で一度も発生していないため。実害（低評価の批評経路のみタイトルが実際に選定・公開される）が観測された時点で再検討する。**Issue #251 はクローズせず監視項目として開けたままにする** |
| 15 | **新作枠の選定フィルタが仕様の60日窓ではなく3ヶ月窓を使っていた**（→ **Issue #260**）。Issue #241 対応（PR #243）でIGDB側の母集団取得クエリは60日窓に揃えられていたが、`scripts/fetch-data.ts` の `selectGamesForArticles` 内の別レイヤーのフィルタ（`buildNewReleaseCandidates` への `releasedAfter` 引数）だけが2026-02-22の古い実装のまま3ヶ月（`setMonth(-3)`、実測約91〜92日）で取り残されていた。管理者が実データ（`data/aggregated.json` 105件）で検証した結果、60日窓の候補14件に対し3ヶ月窓では22件、うち**`Slay the Spire II`（91日前発売）が3ヶ月窓では実際に新作枠に選定されるが60日窓では母集団にすら入らない**実害を確認した。✅ **完了（2026-08-10。PR #261）**。`releasedAfter` を60日窓（`setDate(-60)`）に統一し、境界値回帰テストを追加した |
| 16 | **特集テーマのプレフィルタが候補リストを無制限にプロンプトへ載せていた**（→ **Issue #256**。PR #254の名作枠母集団拡大レビューで検出）。`prefilterFeatureCandidatesByTheme`（`scripts/bedrock-client.ts`）は候補ゲーム全件の`summary`を全文プロンプトに含めており、名作枠母集団拡大（PR #254、IGDBプール全体が123件→288件）で約+34Kトークン/号のコスト増になっていた（実測、2026-08-10）。候補の件数を絞るか（テーマ判定の再現率を落とすリスク）、`summary`を切り詰めるか（既存候補の母集団・選定ロジックは不変）が論点だった。✅ **決定・完了（2026-08-10。PR #264）**: 候補の**件数は変えず**、`summary`のみ`FEATURE_PREFILTER_SUMMARY_MAX_CHARS`（200文字）に切り詰める方針を採用した。`fetch-web-search.ts`の既存の単純`slice`パターンに揃えた |
| 17 | **AI成人向けスクリーニングのfail-openが観測できなかった**（→ **Issue #222**。PR #220のレビューで#221と同時に分離）。`isAdultContentByAI`はBedrock呼び出しが例外を投げると安全側（`false`=非成人向け）へ倒す意図的な仕様だが、失敗回数を記録しないため「成人向けが0件だった正常系」と「Bedrock障害でこの層が事実上無効化された異常系」がログ上で区別できなかった。⚠️ **着手前検証でIssue本文の前提が誤りと判明**: 本文は「AIスクリーニングは特集枠の主防御」としていたが、実際には**IGDBの`themes != (42)`（Erotic）が第1層**（`verifyProposedGames` → `enrichGameWithIGDB({ mainGameOnly: true })` → `searchGameByName`で適用。`aggregated.json`プール側も母集団クエリで通る）、ブロックリストが第2層、AIスクリーニングは**第3層**だった（Issueにコメントで訂正済み）。✅ **完了（2026-08-11。PR #271。マージ `c333eaf`）**: 既存の`WebSearchStats`パターン（カウンタ→`generated-articles.json`永続化→集約警告）を踏襲し、`GenerationStats`に改名の上`adultScreeningFailures`を追加。**観測の出力先が2系統ある**ことをデータフロー追跡で発見し、CIのstdoutだけでなく**永続化されるArticle Validation Report**にも反映（Web検索失敗と同じく`error`ステータス＝`validation`ラベルのIssue自動起票対象に昇格）。`/code-review`で**例外以外の第2のfail-open経路**（`result === 'YES'`の厳密一致のため`YES.`等の応答形式不正が無言で通過。`maxTokens: 10`で切り詰めも起こり得る）を検出し、別カウンタ`unrecognizedScreeningResponses`で計上。ただし**実際のモデル応答形式が未観測**のため誤起票リスクを避けて`error`昇格はさせず、まず実態を観測してから昇格を検討する方針をテストで固定した。**項目2（Bedrock連続呼び出しの間隔）は実害未観測のためスコープ外** |
| 18 | **`fetch-data.ts:1316` の `featured` 選定が特集記事に使われていない**（→ **Issue #289**）。`generateFeatureArticle` の呼び出し（`generate-articles.ts:1421`）に渡されるのは `filteredAllGames`（`:1408-1412` で構築）であり、`selectedGames.featured` ではない。`git log -S` で全履歴を確認したが、`selectedGames.featured` が特集記事生成関数に渡されたことは一度もない。`generate-articles.ts:1393` のコメント「（selectedGames.featured は特集記事自身の素材のため除外しない）」は**事実に反する**。現存する `featured` の効果は3つのみ: ①`fetch-data.ts:1327` の `alreadySelected` に入り**名作枠の重複除外リストになる** ②`fetch-data.ts:676`/`:851` で IGDB/Metacritic の追加取得対象になり**APIコストを消費する** ③`completeness-gate.ts:451` のチェック対象。対処3案: (a) 特集記事に使うよう経路を接続する（§4.5 の「2経路」構造が壊れる可能性） (b) 名作候補から除外する用途として維持（現状維持） (c) ゼロ本許可として削除（API コスト削減）。決着には、(b)/(c) を選ぶなら**先に名作枠候補数への影響を実データで測る**必要がある |

---

## 付録: パラメータ一覧

実装で環境変数化・定数化する数値をまとめます。

### 新作紹介

| パラメータ | 値 |
|---|---|
| 発売済みの探索窓 | 60 日 |
| 未発売の探索窓 | 90 日 |
| 発売済み / 未発売の境界 | 日本時間当日 0 時 |
| 品質条件: 批評媒体数 | 2 以上 |
| 品質条件: ユーザー投票数 | 15 以上 |
| 未発売の注目度フロア | 発売前フォロー数 20 超 |
| 批評軸の信頼度満点 | 媒体数 4 |
| ユーザー票数軸の満点 | 500 票（これ以上は 100 点でクリップ） |
| Steam 軸の分母 | 実際の取得件数（ハードコードしない） |
| 国内販売軸の分母 | 50（掲載枠固定。ノイズ除去後も詰め直さない） |
| 4 軸の初期重み | すべて 1.0 |
| 許可するゲーム種別 | Main Game / リメイク / リマスター |
| クールダウン | 17 週 |

> **✅ 発売済みの探索窓（60 日）はコード側も統一済み**（2026-08-10。PR #261。Issue #260）。
> `scripts/fetch-data.ts` の `selectGamesForArticles` が `buildNewReleaseCandidates` に渡す
> `releasedAfter` が3ヶ月窓（`setMonth(-3)`、実測約91〜92日）のまま本表の60日窓と乖離していたのを
> `sixtyDaysAgo`（`setDate(-60)`）に修正した。IGDB側の母集団取得クエリは Issue #241 対応（PR #243）で
> 既に60日窓に揃っていたため、これで新作枠に関わる全レイヤーが本表の値と一致した。

> **✅ スコア軸の実装で使う環境変数**（2026-08-08。PR #230。再調整は Issue #210）
>
> | 環境変数 | 既定値 |
> |---|---|
> | `NEWRELEASE_SCORE_WEIGHT_CRITIC` / `_VOTES` / `_STEAM` | 各 1.0 |
> | `NEWRELEASE_SCORE_CRITIC_COUNT_MIN` / `_CRITIC_COUNT_FULL` | 2 / 4 |
> | `NEWRELEASE_SCORE_VOTES_MIN` / `_VOTES_FULL` | 15 / 500 |
>
> 実装上の注意: **`Number(x) || 既定値` で読まないこと**（重み 0＝軸の無効化が既定値に化ける）。
> **`VOTES_FULL <= 1` は棄権扱いにする**（`log10(1) = 0` で全候補が 100 点に張り付き、
> 1 未満では符号が反転して軸の順序が逆になるため）。

### インディー

| パラメータ | 値 |
|---|---|
| 探索窓 | 90 日（環境変数 `INDIE_RELEASE_WINDOW_DAYS`。✅ 2026-08-09 に選定側へ実装。それ以前は母集団クエリにしか無く、選定側では効いていなかった） |
| 許可するゲーム種別 | Main Game のみ |
| 大手判定の開発本数閾値 | 20 超（環境変数 `LARGE_STUDIO_DEVELOPED_THRESHOLD`。✅ 2026-08-09 実装） |
| 話題性: Steam おすすめ数 | 5,000 以上 |
| 話題性: Steam ランキング | 200 位以内 |
| ~~話題性: YouTube~~ | **廃止**（→ §3.5・Issue #217。✅ 2026-08-11 実装。PR #273） |
| 並び順 | Steam おすすめ数の降順（→ §3.6） |
| クールダウン | 35 週 |

### 特集

| パラメータ | 値 |
|---|---|
| イベント探索窓（未来方向） | 7 日 |
| イベント 0 件時の遡り上限（過去方向） | 7 日 |
| 最低紹介ゲーム数 | 3 本 |
| クールダウン | 17 週 |

### 名作深掘り

| パラメータ | 値 |
|---|---|
| 総合評価の下限 | 85 |
| 評価母数の下限 | 200 |
| 母集団の取得件数 | 200 |
| 許可するゲーム種別 | Main Game / 原作が母集団に不在のリメイク・リマスター |
| 経過年数の下限 | なし |
| クールダウン | 52 週 |

### 共通

| パラメータ | 値 |
|---|---|
| 成人向けテーマ ID | 42（Erotic） |
| ゲーム種別 ID | Main Game = 0 / リメイク = 8 / リマスター = 9 / 移植 = 11（全枠で除外） |
| 検索結果の抜粋長 | 1,500 字 |
| 記事の文字数 | 800〜1,200 字 |
| 期待記事本数 | 新作 2 / インディー 2 / 特集 1 / 名作 1（計 6） |
