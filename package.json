{
  "name": "game-wire",
  "type": "module",
  "version": "0.0.1",
  "description": "AIを活用した週刊ゲーム情報雑誌の自動生成システム",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch-data": "npx tsx scripts/fetch-data.ts",
    "generate": "npx tsx scripts/generate-articles.ts",
    "build-issue": "npm run fetch-data && npm run generate && npx tsx scripts/build-issue.ts",
    "build-issue:dev": "DEV_MODE=true npm run fetch-data && DEV_MODE=true npm run generate && DEV_MODE=true npx tsx scripts/build-issue.ts",
    "generate-image": "npx tsx scripts/generate-feature-image.ts",
    "test": "vitest run scripts"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.800.0",
    "@tavily/core": "^0.7.2",
    "astro": "^5.17.1",
    "date-fns": "^4.1.0",
    "marked": "^17.0.2",
    "sanitize-html": "^2.17.2"
  },
  "devDependencies": {
    "@aws-sdk/client-bedrock": "^3.990.0",
    "@types/node": "^22.14.0",
    "dotenv": "^17.3.1",
    "tsx": "^4.19.0",
    "vitest": "^4.1.5"
  }
}
